---
title: 05. 高频追问 TOP 30
aliases: [高频追问, TOP 30, 追问清单]
tags: [sprint, 追问, 自测]
status: 完成
updated: 2026-09-16
---

# 05. 高频追问 TOP 30

> 用法：**只看 ⚡ 行自测**；答不出来再看 🎯 行预判下一层。
> 分组：GLM 8 / 树 8 / Boosting 6 / 评估 5 / 统计与业务 3。
> 所有「数字」为经验值；涉及 State Farm 内部信息一律写「未获取到公开信息」。

---

## A. GLM 与逻辑回归（8 题）

### 1. 为什么不用线性回归做分类？

- **⚡ 一句话答案**：线性回归输出是整条实轴上的实数，当概率用会超出 [0,1]；
  而且它用平方损失，在 0/1 标签上**梯度尺度太小、训练困难**、对远离边界的点容错差；
  逻辑回归用 logit link 把均值限制在 (0,1)，用交叉熵（deviance）匹配伯努利方差。
- **🎯 追问会往哪走**：「那用 MSE + sigmoid 行不行？」→ 可以但不好：非凸、梯度消失、
  与指数族/GLM 理论不一致（PSL §10.1）。→ 再追「GLM 的推广是什么？」→ 见 [[1.2 Generalized Linear Models (GLMs)]]。
- 链接：[[1.2 Generalized Linear Models (GLMs)]]、[[1.3 Introducing Logistic Regression]]、[[1.4 Model Fitting]]

### 2. logit 和 probit 的区别？怎么选？

- **⚡ 一句话答案**：两者都是把线性预测映射到概率的 link，
  **logit**（canonical，`ln(p/(1−p))`）有 odds ratio 的直观解释、尾部更厚；
  **probit**（正态 CDF 逆函数）在经济学/潜在变量模型里常用。
  实际拟合的概率曲线差别很小，只在极端尾部有差别。
- **🎯 追问会往哪走**：「系数能解释成 odds 吗？」→ probit 不能，logit 可以。
  → 「为什么 logit 是 canonical？」→ 因为它让 `g(μ)=η` 与自然参数一致，使似然更简单、凸。
- 链接：[[1.2 Generalized Linear Models (GLMs)]]、[[1.3 Introducing Logistic Regression]]

### 3. 逻辑回归的系数怎么解释？`exp(β)` 是什么？

- **⚡ 一句话答案**：β_j 是 x_j 每增加 1 单位、**其余变量不变**时 **log-odds 的变化**；
  `exp(β_j)` 是 odds ratio，即赔率变成原来的几倍。连续变量说「1 单位」，
  分箱/WOE 变量说「从第 k−1 箱到第 k 箱」；类别变量是相对基准组的倍数。
- **🎯 追问会往哪走**：「odds 和概率有什么差别？」→ 低概率时近似相等、高概率差异大。
  → 「交互项怎么解释？」→ 主效应解释变成「另一变量为 0 时」的条件效应，必须一起看图。
  → 「置信区间怎么看？」→ `exp(β ± z·SE)`，区间跨 1 就不显著。
- 链接：[[1.3 Introducing Logistic Regression]]、[[2.3.1 WOE & IV]]

### 4. 完全分离怎么办？Ridge / Lasso 能解决吗？

- **⚡ 一句话答案**：完全分离时存在超平面把两类完美分开，似然可以趋近上界但
  **β 无界、不收敛**，SE 巨大，结果无意义。**Ridge/Lasso 只能缓解，不能真正解决**
  （CV 常选 λ=0，等于没惩罚）；**Firth 惩罚**（`ℓ(β)+½log det I(β)`，Jeffreys prior，
  R 的 `logistf`）给出**有限**估计。其他替代：合并稀疏类别、删掉分离变量、增大样本。
- **🎯 追问会往哪走**：「为什么 Ridge 不行？」→ 惩罚是有偏的收缩，优化仍会朝大 β 推，
  CV 看不到惩罚收益。→ 「分离但决策边界稳定吗？」→ **稳定**，预测排序照样可用，
  只是概率和推断不可信。→ 「EPV 多少才安全？」→ 10–20（经验值）。
- 链接：[[1.6 Estimation Issues & Separation]]、[[1.6.3 Regularization]]

### 5. 逻辑回归为什么用交叉熵 / deviance，而不是平方损失？

- **⚡ 一句话答案**：不是因为平方损失「有偏」，而是 `(y−η)²` 在概率尺度上
  **数值太小、梯度太平**，训练慢且对边界样本不敏感；
  交叉熵（= 负对数似然）是对应 Bernoulli 分布的规范损失，
  与 GLM/指数族理论一致，似然还是凸的。
- **🎯 追问会往哪走**：「那 GBM 里用平方损失训练分类行不行？」→ 可以但不优，
  常用 deviance/对数损失。→ 「损失和 link 是什么关系？」→ canonical link 让二者自洽。
- 链接：[[1.4 Model Fitting]]、[[1.2 Generalized Linear Models (GLMs)]]、[[8.4.2.2 Gradient Boosting]]

### 6. 逻辑回归的假设是什么？要检查残差正态吗？

- **⚡ 一句话答案**：要的是 ① 观测独立 ② **logit 尺度上的线性** ③ 正确指定模型（含缺失/分箱）。
  **不要求**残差正态、不要求同方差、不要求 y 连续。诊断看：分箱残差/偏差、
  影响点（Cook 距离）、Hosmer–Lemeshow、校准曲线。
- **🎯 追问会往哪走**：「非线性怎么办？」→ 分箱/WOE、样条、交互项。
  → 「异常值和共线性呢？」→ 影响点要单独看，共线性伤害解释，见 [[3.4.2 Variance Inflation Factor (VIF)]]。
- 链接：[[1.7 Assumptions]]、[[1.3 Introducing Logistic Regression]]、[[6.4 Diagnostic & Visualization Tools]]

### 7. 类别不平衡怎么处理？SMOTE 之后能直接用概率吗？

- **⚡ 一句话答案**：顺序是 ① 先换指标（AUPRC、Recall@K、Lift，而不是 accuracy）
  ② 再用类权重/阈值调整 ③ 必要时重采样（SMOTE 等）。
  **SMOTE 之后概率被系统性抬高，必须校准**（Platt / isotonic，或按先验比例做截距校正），
  评分卡场景尤其忌讳直接输出重采样后的概率。
- **🎯 追问会往哪走**：「逻辑回归和树谁更受影响？」→ 逻辑回归的截距直接反映先验，
  影响最明显。→ 「采样要不要放进 CV 内部？」→ 必须，只在训练折上做，否则泄漏。
- 链接：[[1.5 Dealing with Unbalanced Samples]]、[[6.1 Data Preparation & Validation]]、[[6.3 Performance Metrics]]

### 8. 病例对照 / 回顾性抽样后，逻辑回归还能用吗？

- **⚡ 一句话答案**：能用。逻辑回归对**给定 X 的 Y 抽样**是稳健的：
  除**截距**外系数几乎不变，截距可以用已知的先验比例做校正（offset）。
  所以罕见事件可以用「全部正类 + 抽样负类」来省数据。
- **🎯 追问会往哪走**：「那概率还能直接用吗？」→ 截距未校正不能当真实风险。
  → 「树模型有这个性质吗？」→ 没有，树模型的输出会被采样比例直接影响。
- 链接：[[1.6 Estimation Issues & Separation]]、[[1.5 Dealing with Unbalanced Samples]]

---

## B. 树与随机森林（8 题）

### 9. 树模型为什么不需要标准化？

- **⚡ 一句话答案**：树只在**单个特征的一个维度**上按阈值切分，比较的是**值的顺序**，
  单调变换（加减乘、log）不改变排序，因而不改变分裂结果。
  线性模型/正则化/距离模型（KNN、SVM、聚类）才需要标准化。
- **🎯 追问会往哪走**：「那树完全不用预处理？」→ 仍要处理缺失（部分实现可原生处理）、
  异常值（影响切分但仍鲁棒）、类别编码与高基数问题。
- 链接：[[7.2.2 Decision Tree]]、[[7.5 CART Algorithm]]

### 10. 熵、Gini、误分类率有什么区别？选哪个？

- **⚡ 一句话答案**：三者都是不纯度度量，二分类下最大值分别是 1 bit、0.5、0.5。
  **熵和 Gini 是严格凹的**，分裂增益恒 > 0，因此鼓励纯节点；
  **误分类率是分段线性的**，只要两个子节点落在 p=0.5 的同一侧，增益就是 0，
  无法区分有效与无效分裂。所以**建树用熵/Gini，误分类率只在剪枝/评估时用**。
- **🎯 追问会往哪走**：「Gini 和熵选哪个？」→ 数值接近，熵计算含 log 略慢，
  Gini 是 CART 默认。→ 「为什么增益恒正？」→ 凹函数的 Jensen 不等式，
  按比例加权平均不纯度 < 父节点不纯度。
- 链接：[[7.2.2 Decision Tree]]、[[7.5 CART Algorithm]]、[[7.2.2.4. Pruning Decision Trees]]

### 11. 为什么误分类率不能用来建树？（第 10 题的反面）

- **⚡ 一句话答案**：它在 p=0.5 附近**没有梯度信息**（分段线性、平坦），
  只要候选分裂不改变「多数类」，增益就是 0，导致算法看不到改进方向；
  而熵/Gini 是严格凹，能区分「好一点」和「好很多」。剪枝时用误分类率是可以的
  （因为已经不做选择了，只看子树代价）。
- **🎯 追问会往哪走**：「那 C4.5 为什么提增益率？」→ 信息增益偏向取值多的特征，
  增益率用 `SplitInfo` 惩罚它。
- 链接：[[7.2.2 Decision Tree]]、[[7.2.3.3 C4.5 Algorithm]]、[[7.2.2.4. Pruning Decision Trees]]

### 12. 决策树怎么防止过拟合？预剪枝 vs 后剪枝？

- **⚡ 一句话答案**：预剪枝（提前停：max_depth、min_samples_leaf、min_impurity_decrease）
  快、便宜，但贪心早停可能错过好分裂；后剪枝（先长大树再剪）效果好、计算贵。
  CART 的标准做法是**代价复杂度剪枝**：`R_α(T) = R(T) + α|T|`，
  weakest-link 生成 α 路径，再用 **K 折 CV 选 α**，可用 **1-SE 原则**选更简单的树。
- **🎯 追问会往哪走**：「α 变大树怎么变？」→ 树更小。→ 「为什么剪枝优于预剪枝？」
  → 先看到整体结构再判断哪些分支没用，少了贪心的近视。
- 链接：[[7.2.2.4. Pruning Decision Trees]]、[[7.5 CART Algorithm]]

### 13. 为什么随机森林要随机选特征？（`m` 怎么定？）

- **⚡ 一句话答案**：只做 bootstrap 平均还不够——树间相关 ρ 高时
  `Var = ρσ² + (1−ρ)σ²/B` 里第二项压不下去，平均收益有限。
  随机抽特征（分类 m=√p、回归 m=p/3）**强制降低 ρ**，用一点点偏差换大幅方差下降。
- **🎯 追问会往哪走**：「m 太小/太大分别怎样？」→ m 小 → 树更独立、方差更低但偏差升；
  m=p 就退化成 bagging。→ 「B 要多大？」→ 到 OOB 误差平稳即可，不是越大越好（边际收益递减）。
- 链接：[[7. Random Forest]]、[[7.2. Building the Forest]]、[[8.3. Bagging (Bootstrap Aggregating)]]

### 14. OOB 误差怎么算？为什么说是「免费的验证集」？

- **⚡ 一句话答案**：每个 bootstrap 样本约有 `1−(1−1/n)^n → 36.8%` 没被抽到；
  对这些 OOB 样本用**未见过它们**的树做预测并平均，就得到每行样本的 OOB 预测，
  再算指标。整个过程不需要额外切分数据，所以是免费的验证集。
- **🎯 追问会往哪走**：「OOB 和 CV 谁更可信？」→ 相似；数据有**时间结构**时
  两者都可能乐观，必须按时间切分。→ 「OOB 能用来调参吗？」→ 可以，
  相当于内部验证，但最终仍要留独立测试集。
- 链接：[[7.2. Building the Forest]]、[[8.3. Bagging (Bootstrap Aggregating)]]、[[6.1 Data Preparation & Validation]]

### 15. MDI 和 permutation importance 谁更可信？

- **⚡ 一句话答案**：**MDI**（RSS/杂质下降）快、训练时顺带得到，
  但被**高基数/多取值特征夸大**（分裂机会多），且是在训练集上算的；
  **permutation importance** 在**OOB/验证集**上打乱某一列再看指标掉多少，
  更贴「预测贡献」，但相关特征会互相分摊（都显得不重要）。
  跨基数比较用 permutation，MDI 只在同基数内看趋势。
- **🎯 追问会往哪走**：「还有什么办法？」→ 单变量 drop-column、SHAP、
  条件置换（conditional permutation）。→ 「为什么相关的特征重要度都被压低？」→
  打乱其中一个时另一个仍能提供信息。
- 链接：[[7.4. Quantifying Feature Importance]]、[[7. Random Forest]]

### 16. 随机森林会过拟合吗？树长多深？

- **⚡ 一句话答案**：**随 B 增加不会过拟合**（平均只会收敛），但**单树很深 +
  标签噪声 + 样本很小**时整体仍会过拟合。实践中让树长深（低偏差）、
  用 m 和 `min_samples_leaf` 控制，再用 OOB/CV 验证。
- **🎯 追问会往哪走**：「那为什么还会过拟合？」→ 噪声样本被深树记住，
  平均只是削弱而不是消除。→ 「和 GBM 比呢？」→ GBM 更敏感，需要早停与学习率。
- 链接：[[7.2. Building the Forest]]、[[6.2 Model Diagnosis_Bias-Variance Tradeoff]]、[[8.6 Bagging VS Boosting]]

---

## C. Boosting（6 题）

### 17. AdaBoost 的 `α_t` 为什么长这样？

- **⚡ 一句话答案**：`α_t = ½·ln((1−ε_t)/ε_t)` 是最小化指数损失得到的闭式解：
  ε_t 越小（越准）α_t 越大，投票越有分量；ε_t = 0.5 时 α_t = 0（没用）；
  **ε_t > 0.5 时 α_t 为负**，等于反向投票。权重更新
  `D_{t+1}(i) ∝ D_t(i)·exp(−α_t·y_i·h_t(x_i))`，让错分样本权重上升。
- **🎯 追问会往哪走**：「和指数损失什么关系？」→
  AdaBoost = 指数损失 `exp(−y·f(x))` 下的**前向分步加性模型**。
  → 「对噪声敏感吗？」→ 敏感，因为错误样本权重会被反复放大。
- 链接：[[8.4.2.1 AdaBoost (Adaptive Boosting)]]、[[8.4.2.1.4 AdaBoost Formulas]]、[[8.4. Boosting]]

### 18. AdaBoost 的训练误差一定单调下降吗？

- **⚡ 一句话答案**：**不一定**。理论保证下降的是上界
  `∏_t 2√(ε_t(1−ε_t)) < 1`；实际的 0/1 训练误差不保证每轮都降
  （尤其在有噪声标签或 ε_t 接近 0.5 时）。
- **🎯 追问会往哪走**：「那怎么监控？」→ 看验证集损失曲线 + 早停。
  → 「为什么会这样？」→ 加性模型每步只做贪心优化，单步不一定改善当前指标。
- 链接：[[8.4.2.1.4 AdaBoost Formulas]]、[[8.4.2.1 AdaBoost (Adaptive Boosting)]]、[[6.2 Model Diagnosis_Bias-Variance Tradeoff]]

### 19. GBM 里的「残差」是什么？为什么叫伪残差？

- **⚡ 一句话答案**：`r_i = −∂L(y_i, f(x_i))/∂f(x_i)`，即**损失对当前预测的负梯度**。
  平方损失下它正好是 `y_i − f(x_i)`（普通残差）；换成对数损失/其它损失时
  它只是个梯度方向，所以叫**伪残差**。每轮用树去拟合它，再加进模型：
  `F_m = F_{m−1} + ν·h_m`。
- **🎯 追问会往哪走**：「ν 的作用？」→ shrinkage，缩小每步贡献，防止过早过拟合。
  → 「为什么是拟合残差而不是直接调参？」→ 前向分步加性模型的贪心策略。
- 链接：[[8.4.2.2 Gradient Boosting]]、[[8.4. Boosting]]

### 20. 学习率（eta）和树数 T 什么关系？

- **⚡ 一句话答案**：**反比**。ν 越小，每棵树贡献越小，需要更多树 T 才能达到同样的
  训练损失；但泛化通常更好（更接近精细搜索）。ν 大则收敛快、容易过拟合且曲线糙。
  实践：ν 取 0.01–0.1（经验值），配早停或 CV 定 T，树要浅（depth 2–6 常见），
  再加子采样（行/列）降方差。
- **🎯 追问会往哪走**：「怎么调？」→ 网格/随机搜索 + 早停（验证损失不再降就停）。
  → 「树深和 T 的关系？」→ 树浅要多轮，树深单轮强但易过拟合。
- 链接：[[8.4.2.2 Gradient Boosting]]、[[6.1 Data Preparation & Validation]]

### 21. Bagging 和 Boosting 的本质区别？

- **⚡ 一句话答案**：Bagging **并行**训练相互独立的树并平均，主要**降方差**；
  Boosting **串行**训练，每个模型拟合前面的错误/梯度，主要**降偏差**，
  代价是方差随轮数可能上升。所以 Bagging 调参少、抗噪；Boosting 调好更强、更敏感。
- **🎯 追问会往哪走**：「哪个更容易过拟合？」→ Boosting（尤其 eta 大、T 多、树深）。
  → 「RF 的偏差呢？」→ 和单树相当甚至略高。
- 链接：[[8.6 Bagging VS Boosting]]、[[8. Ensemble Learning]]、[[8.3. Bagging (Bootstrap Aggregating)]]

### 22. 随机森林和 GBM 怎么选？面试里怎么说取舍？

- **⚡ 一句话答案**：要**少调参、稳定、可并行、抗噪** → 随机森林；
  要**最优精度、能调参、接受训练成本** → GBM。数据不大/特征噪声多时 RF 更省心，
  高维强信号时 GBM 通常赢；两者都基于树，都不能外推，且都需要把
  **校准 + 漂移监控（PSI）** 补齐再上线。
- **🎯 追问会往哪走**：「保险场景里谁更常用？」→ 通用经验是 GLM/评分卡做解释性强的
  定价与核保，树/GBM 用于欺诈、续保、赔付倾向，并配 SHAP/部分依赖解释。
  （State Farm 内部具体做法**未获取到公开信息**。）
- 链接：[[8.6 Bagging VS Boosting]]、[[7. Random Forest]]、[[8.4.2.2 Gradient Boosting]]、[[10. 保险风控考点总览]]

---

## D. 评估与数据（5 题）

### 23. AUC 高就说明模型好吗？

- **⚡ 一句话答案**：不一定。AUC 只衡量**全局排序**能力，阈值无关，
  与业务代价、校准、稳定性都脱钩。要看：业务目标对应指标（Recall@K/Lift/成本）、
  概率校准（可靠性图）、PSI 漂移、以及**是否在时间外样本**上评估。
- **🎯 追问会往哪走**：「什么时候 AUC 会骗人？」→ 极不平衡（正类极少）时 AUC 看着还行
  但 precision 很差。→ 「替代？」→ AUPRC、KS、Lift。
- 链接：[[6.3 Performance Metrics]]、[[6.4 Diagnostic & Visualization Tools]]

### 24. AUC 和 KS 什么关系？Gini 怎么算？

- **⚡ 一句话答案**：AUC = 随机取一个正类一个负类、正类得分更高的概率；
  KS = 正负类累计分布函数之间的**最大距离**。两者都在衡量区分度、高度相关
  （同数据下 KS 常约等于 AUC 与 0.5 之差的两倍量级，但二者不相等）。
  `Gini = 2·AUC − 1`，风控里常报 KS 和 Gini。
- **🎯 追问会往哪走**：「KS 多少算好？」→ 经验值 > 0.3 可用（场景相关）。
  → 「不平衡时会怎样？」→ 两者都不敏感，配 AUPRC 一起看。
- 链接：[[6.3 Performance Metrics]]

### 25. 不平衡数据为什么不看 accuracy？AUPRC 怎么读？

- **⚡ 一句话答案**：正类 1% 时全预测负类就有 99% accuracy，但模型毫无价值。
  AUPRC 的**baseline 等于正类比例**（不是 0.5）；正类极稀有且关心前段命中率时，
  PR 曲线比 ROC 更能反映实际可用性。
- **🎯 追问会往哪走**：「AUPRC 怎么调阈值用？」→ 结合业务代价选阈值或看 Recall@K。
  → 「阈值怎么挑？」→ 按成本曲线/期望损失，而不是按 0.5。
- 链接：[[6.3 Performance Metrics]]、[[1.5 Dealing with Unbalanced Samples]]

### 26. 验证集和测试集是一回事吗？怎么切分才不泄漏？

- **⚡ 一句话答案**：不是。验证集用于选模型/调参，测试集只在最后用一次。
  防泄漏要点：① 任何预处理（标准化、WOE 分箱、SMOTE、缺失插补）
  **只在训练折上拟合**，再 transform 验证折；② 分组数据要按实体切分；
  ③ 有时间结构必须按**时间切分**（或用 rolling window / purged CV）；
  ④ 调完超参用嵌套 CV 报最终表现，避免选择偏差。
- **🎯 追问会往哪走**：「调参用测试集会怎样？」→ 指标偏乐观，等于泄漏。
  → 「交叉验证怎么做才不泄漏？」→ 把流水线包进 CV，而不是先全局做变换。
- 链接：[[6.1 Data Preparation & Validation]]、[[6.2 Model Diagnosis_Bias-Variance Tradeoff]]

### 27. PSI 是什么？多少算不稳定？

- **⚡ 一句话答案**：`PSI = Σ (A_i − E_i)·ln(A_i/E_i)`，按分箱比较
  当前分布（A）与基准分布（E）的偏移，类似对称化的 KL 散度。
  经验阈值：**<0.1 稳定，0.1–0.25 需关注，>0.25 显著漂移**
  （行业经验值，不同团队口径不同）。它是监控项，不代表模型一定失效。
- **🎯 追问会往哪走**：「发现 PSI 高怎么办？」→ 先看是数据管道问题还是真实漂移，
  再考虑重训/重新分箱/加保护变量。→ 「和 KS 一起看？」→ PSI 看输入，KS/召回看输出。
- 链接：[[10. 保险风控考点总览]]、[[2.3.1 WOE & IV]]、[[6.3 Performance Metrics]]

---

## E. 统计与业务（3 题）

### 28. p 值能说明效应大小吗？

- **⚡ 一句话答案**：不能。p 值是在 H₀ 下观测到至少这么极端结果的概率，
  **不是「效应为零的概率」**，也随样本量变化——大样本下极小效应也能显著。
  报告时给**效应量 + 置信区间**，CI 比 p 值信息更多。
- **🎯 追问会往哪走**：「那比较多个特征/模型要注意什么？」→ **多重比较**：
  用 Bonferroni（α/m）或 FDR 校正，或者用**嵌套 CV / 同一折比较**，
  否则挑出来的最佳模型会因**选择偏差**而表现虚高。
- 链接：[[1.7 Assumptions]]、[[1.1 Ordinary Least Squares (OLS)]]、[[6.1 Data Preparation & Validation]]

### 29. VIF 高到底要不要删变量？

- **⚡ 一句话答案**：看目的。VIF 高意味着系数 SE 膨胀、符号不稳，
  **伤害解释与推断**；但纯预测（尤其树/集成）几乎不受影响。
  要解释 → 合并/删除/用正交化或 Ridge，并说明处理理由；
  要预测 → 可以保留。VIF > 5 关注、> 10 严重是**经验值**，不是自动删除令。
- **🎯 追问会往哪走**：「VIF 公式？」→ `1/(1−R²_j)`，用其余变量回归 x_j 的 R²。
  → 「还有什么诊断？」→ 条件数、相关矩阵、VIF 矩阵；组内相关用 VARCLUS（了解即可）。
- 链接：[[3.4.2 Variance Inflation Factor (VIF)]]、[[3. Multicollinearity]]、[[5.3 Multivariate Selection]]

### 30. WOE 为什么要求单调？IV 阈值怎么用？

- **⚡ 一句话答案**：WOE 是把分箱映射成 log 似然比，若 WOE 随分箱**非单调**，
  说明分箱没抓住真实趋势、可能是噪声或样本太少，会破坏评分卡的可解释性与稳定性；
  通常合并相邻箱直到单调。IV = Σ(dist_good − dist_bad)·WOE 衡量单变量预测力，
  经验参考：<0.02 无用、0.02–0.1 弱、0.1–0.3 中、0.3–0.5 强、>0.5 可能过拟合或泄漏。
- **🎯 追问会往哪走**：「WOE 越高风险越高吗？」→ 取决于正负类定义，
  必须确认约定；且 WOE 编码后逻辑回归系数应为**同向**。
  → 「IV 高就一定要用吗？」→ 还要看稳定性、业务合理性与多重比较。
- 链接：[[2.3.1 WOE & IV]]、[[2. Transformations]]、[[10. 保险风控考点总览]]

---

## 自测清单

- [ ] 30 个 ⚡ 能在 30 秒内说出口，且不背稿
- [ ] 每个 🎯 的下一层追问能接住一层
- [ ] 数字类阈值都带「经验值」三个字
- [ ] 涉及内部信息能说「未获取到公开信息」
- [ ] 用 [[03. 答题模板与追问应对]] 的六步结构复述 3 题
- [ ] 公式全部落在 [[04. 公式速查卡]] 上

---

## 相关入口

- 岗位与权重：[[01. 面试画像与考点分布]]
- 学习路径：[[02. 三条学习路径]]
- 答题模板：[[03. 答题模板与追问应对]]
- 公式卡：[[04. 公式速查卡]]
- 题库：[[99.1 题库总览]]
