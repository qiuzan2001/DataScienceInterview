---
title: 04. 公式速查卡
aliases: [公式速查, 公式卡, formula sheet]
tags: [sprint, 公式, 速查]
status: 完成
updated: 2026-09-16
---

# 04. 公式速查卡

> 一页纸，面试前 30 分钟只看这个。**所有「阈值」都是经验值**，不同机构口径不同，面试时说明「经验值是 X」即可。
> 列含义：`公式` | `含义` | `关键数字/阈值（经验值）` | `对应笔记`。

---

## 1. GLM 与逻辑回归

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `ln(p/(1−p)) = β₀ + β₁x₁ + …` | logit（对数几率）的线性模型：把 odds 的对数写成特征的线性组合，这就是逻辑回归的核心 | β 的尺度：每单位 x 改变 log-odds | [[1.3 Introducing Logistic Regression]] |
| `p = 1/(1+e^{−Xβ})` | sigmoid 把线性预测 η 压到 (0,1)，这是「概率」而不是「分数」的来源 | η=0 → p=0.5 | [[1.3 Introducing Logistic Regression]] |
| `OR_j = exp(β_j)` | x_j 增加 1 个单位时 odds 变成原来的几倍；系数取指数就是它 | OR=1 无效应；0.5/2 是等量级 | [[1.3 Introducing Logistic Regression]] |
| `exp(β_j ± z_{1−α/2}·SE_j)` | odds ratio 的置信区间：先算 β 的 Wald 区间，再整体取指数 | 用 Wald CI（大样本） | [[1.3 Introducing Logistic Regression]] |
| `μ = b′(θ)`，`Var(Y) = φ·b″(θ)` | 指数族（exponential family）的核心性质：均值与方差都由累积函数 b(θ) 的导数给出 | 正态 / 二项 / Poisson / Gamma 均属此族 | [[1.2 Generalized Linear Models (GLMs)]] |
| `g(μ) = Xβ` | link 函数把均值 μ 连到线性预测 η = Xβ；GLM 的三件套就是「分布 + link + 线性预测」 | 二项→logit，Poisson→log，Gamma→inverse | [[1.2 Generalized Linear Models (GLMs)]] |
| `ℓ(β)=Σ[y·ln p+(1−y)·ln(1−p)]` | 对数似然：最大化它就是 MLE；与交叉熵只差一个符号 | 用 MLE 而非最小二乘 | [[1.4 Model Fitting]] |
| `D = 2[ℓ_sat − ℓ(β)]` | deviance（偏差）：当前模型离**饱和模型**的差距，用来衡量拟合好坏 | 嵌套模型比较用；近似 χ² | [[1.2 Generalized Linear Models (GLMs)]] |
| `AIC = −2ℓ + 2k`，`BIC = −2ℓ + k·ln n` | 信息准则：在拟合与复杂度之间做取舍，数值越小越好，用来比较模型 | BIC 罚更重（k·ln n） | [[1.6.3 Regularization]] |
| `ℓ(β) + ½·log det I(β)` | **Firth 惩罚**（Jeffreys prior）：在似然上加一项，用来治分离问题、给出有限估计 | 完全分离时给**有限估计**，R 的 `logistf` | [[1.6 Estimation Issues & Separation]] |
| 分离时 `β → ∞`，对数似然 → 0 | 完全/准分离：数据能被一个平面完全分开，此时 MLE 不存在（`ℓ` 逼近 0 但取不到） | Ridge/Lasso 只缓解，CV 常选 λ=0；Firth 给有限估计 | [[1.6 Estimation Issues & Separation]] |
| 回顾性/病例对照抽样：除截距外系数不变 | 回顾性 / 病例对照抽样：逻辑回归对**给定 X 的 Y 抽样**免疫 —— 只有截距受先验变化影响，**斜率与排序指标几乎不动** | 截距可用 offset 校正 | [[1.6 Estimation Issues & Separation]] |
| 不用 `(y−η)²`（L2 损失） | 为什么不用平方损失：它在概率尺度上**梯度太平、尺度太小**（不是「有偏」） | 所以用交叉熵 / deviance | [[1.4 Model Fitting]] |

---

## 2. 正则化

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `Ridge: min ‖y−Xβ‖² + λ·Σβ_j²` | Ridge（L2 惩罚）：把系数整体往零收缩、但不置零，共线性时用来稳住系数 | λ↑ → β↓；需标准化 | [[1.6.3 Regularization]] |
| `Lasso: min ‖y−Xβ‖² + λ·Σ abs(β_j)` | Lasso（L1 惩罚）：会把一部分系数压到恰好为 0，因此自带变量选择 | λ 大到一定值全部置 0 | [[1.6.3 Regularization]] |
| `ElasticNet: min … + λ[α·Σ abs(β_j) + (1−α)·Σβ_j²]` | ElasticNet：L1 与 L2 的混合，既稀疏又稳，变量彼此相关时特别合适 | α=1 → Lasso，α=0 → Ridge | [[1.6.3 Regularization]] |
| `CV 选 λ`：λ_min 或 **1-SE 规则** | 用交叉验证来挑惩罚强度 λ；1-SE 规则偏向更简单（更保守）的模型 | 1-SE：选最简模型，误差在 1 个 SE 内 | [[1.6.3 Regularization]]、[[6.1 Data Preparation & Validation]] |
| 标准化必要性 | 惩罚项对变量尺度敏感，所以要先标准化；哑变量通常不参与惩罚 | 先 `(x−μ)/σ`；哑变量通常不罚 | [[1.6.3 Regularization]] |
| 正则化 ≠ 解决分离 | 正则化不等于解决分离：它只能**缓解**，分离下 CV 常把 λ 选回 0 | 要**有限估计**用 Firth | [[1.6 Estimation Issues & Separation]] |

---

## 3. 树与不纯度

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `H(t) = −Σ_k p_k·log₂ p_k` | 熵（entropy）：节点的不纯度度量，越「乱」越大 | 二分类最大 1 bit（p=0.5）；纯节点 0 | [[7.2.2 Decision Tree]] |
| `Gini(t) = 1 − Σ_k p_k²` | Gini 不纯度：本质是「任取两个样本、类别不同」的概率 | 二分类最大 0.5（p=0.5） | [[7.5 CART Algorithm]] |
| `Err(t) = 1 − max_k p_k` | 误分类率：节点里少数类的占比，最直观但对分裂不敏感 | 二分类最大 0.5；**分段线性** | [[7.2.2 Decision Tree]] |
| 熵/Gini 严格凹 → 分裂增益恒 > 0 | 熵 / Gini 的严格凹性保证分裂增益恒为正，所以**建树要用它们** | **建树用熵/Gini** | [[7.2.2 Decision Tree]] |
| 误分类率**分段线性**：两子节点同在 p=0.5 一侧 → 增益 = 0 | 误分类率是分段线性的，会把「有改进」和「没改进」的分裂看成一样 | **剪枝可用误分类率** | [[7.2.2.4. Pruning Decision Trees]] |
| `Δ = H(parent) − Σ (n_child/n_parent)·H(child)` | 信息增益（ID3 的分裂准则）：父节点熵减去子节点熵的加权平均 | 偏好多值特征 → C4.5 修正 | [[7.2.2.3.1 ID3 Algorithm]] |
| `GainRatio = Δ / SplitInfo` | 增益率（C4.5）：用分裂自身的熵做分母，惩罚取值特别多的特征 | SplitInfo = 分裂自身的熵 | [[7.2.3.3 C4.5 Algorithm]] |
| `ΔI_t(j) = I(S_t) − (n_L/n_t)·I(S_L) − (n_R/n_t)·I(S_R)` | 杂质下降（impurity decrease）：把所有分裂累加起来就得到 MDI 特征重要性 | 累加后即 MDI 重要性 | [[7.4. Quantifying Feature Importance]] |
| `R_α(T) = R(T) + α·n_T` | **代价复杂度（CCP）**：把「树的误差 + 复杂度惩罚」合成一个剪枝准则 | α=0 → 大树；α↑ → 更小树 | [[7.2.2.4. Pruning Decision Trees]] |
| weakest-link：每步剪掉使 `R_α` 上升最小的子树 | weakest-link 剪枝：每步剪掉「最不划算」的子树，从而生成一串嵌套的树 | 用 **K 折 CV 选 α**，可选 **1-SE 原则** | [[7.2.2.4. Pruning Decision Trees]]、[[7.5 CART Algorithm]] |
| 树**不需要**标准化 | 树不需要标准化：分裂只看排序与阈值，单调变换不改变结果 | 单调变换不变 | [[7.2.2 Decision Tree]] |

---

## 4. 集成与 Boosting

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `Var = ρσ² + (1−ρ)σ²/B` | Bagging 之后集成的方差：B 越大第二项越小，但 `ρσ²` 是压不下去的地板 | 关键降 **ρ**（树间相关）；B↑ 第二项 → 0 | [[8.3. Bagging (Bootstrap Aggregating)]] |
| `P(样本不在 bootstrap 中) = (1−1/n)^n → e^{−1} ≈ 0.368` | bootstrap 抽不到某个样本的概率 ≈ **36.8%**，其余 **63.2%** 被抽到；这些 OOB 样本可以当验证集用 | OOB 当验证集用 | [[8.3. Bagging (Bootstrap Aggregating)]] |
| RF 的 `m`：分类 `√p`，回归 `p/3` | RF 每次分裂随机抽的特征数 m：它决定树之间的相关性 ρ | m↓ → 树更独立、偏差略升 | [[7.2. Building the Forest]] |
| MDI（RSS gain） vs permutation（在 **OOB** 上置换） | 两种特征重要性：MDI 是树内杂质下降的累加，permutation 是在 OOB 上打乱特征 | 高基数变量在 MDI 被**夸大** | [[7.4. Quantifying Feature Importance]] |
| AdaBoost 加权误差 `ε_t = Σ w_i·1[y_i≠h_t(x_i)] / Σ w_i` | AdaBoost 第 t 轮的加权错误率：样本权重跟着上一轮错分的样本走 | 要求弱学习器 ε_t < 0.5 | [[8.4.2.1 AdaBoost (Adaptive Boosting)]] |
| `α_t = ½·ln((1−ε_t)/ε_t)` | 该学习器的投票权重：错得越少、权重越大 | ε_t→0 → α_t 大；**ε_t>0.5 → α_t<0** | [[8.4.2.1.4 AdaBoost Formulas]] |
| `D_{t+1}(i) ∝ D_t(i)·exp(−α_t·y_i·h_t(x_i))`，归一化 `/Z_t` | 样本权重更新：上一轮被错分的样本，这一轮权重变大 | 错分样本权重上升 | [[8.4.2.1 AdaBoost (Adaptive Boosting)]] |
| 训练误差上界 `∏_t 2√(ε_t(1−ε_t)) < 1` | 理论上界：只要每个弱学习器都略好于随机，这个上界就随轮数下降 | **实际 0/1 误差不保证单调下降** | [[8.4.2.1.4 AdaBoost Formulas]] |
| AdaBoost = **指数损失**下的前向分步加性模型 | AdaBoost 在统一视角下就是「指数损失下的前向分步加性模型」 | 损失 `exp(−y·f(x))` | [[8.4. Boosting]]、[[8.6 Bagging VS Boosting]] |
| GBM：`r_i = −[∂L(y_i,f(x_i))/∂f(x_i)]` | GBM 的**伪残差**（pseudo-residual）：损失对当前预测的负梯度 | 平方损失时即普通残差 | [[8.4.2.2 Gradient Boosting]] |
| `F_m(x) = F_{m−1}(x) + ν·h_m(x)` | 加性更新式：新模型 = 旧模型 + 学习率 × 新树（shrinkage 就在这一步） | ν = eta，步长 | [[8.4.2.2 Gradient Boosting]] |
| eta 与树数 T **反比** | 学习率与树数成反比：eta 越小、每步走得越短，就需要越多树 | eta 常见 0.01–0.1；树要**浅**；加子采样 | [[8.4.2.2 Gradient Boosting]] |
| RF 调参少 / GBM 调好更强 | 选型结论：RF 几乎不用调参、天然并行；GBM 调参成本高，但天花板更高 | RF 并行、鲁棒；GBM 串行、精度高 | [[8.6 Bagging VS Boosting]] |

---

## 5. 评估指标

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `Precision = TP/(TP+FP)`，`Recall = TP/(TP+FN)` | 精确率与召回率：判为正的准不准、真正发生的事件抓到多少 | 阈值相关，二选一要看业务代价 | [[6.3 Performance Metrics]] |
| `F1 = 2PR/(P+R)` | F1：精确率与召回率的调和平均，用一个数把两者平衡起来 | 不平衡时比 accuracy 有意义 | [[6.3 Performance Metrics]] |
| `Accuracy = (TP+TN)/N` | 整体正确率：类别不平衡时会被多数类主导，看起来很漂亮 | **不平衡数据下会骗人** | [[6.3 Performance Metrics]] |
| ROC 曲线 / `AUC = P(score_正 > score_负)` | AUC 的等价定义：随机取一个正样本与一个负样本，正样本得分更高的概率 | 0.5=随机；0.7–0.8 可接受（经验值，看场景） | [[6.3 Performance Metrics]] |
| `Gini = 2·AUC − 1` | Gini 与 AUC 的换算关系：Gini = 2·AUC − 1 | AUC 0.8 → Gini 0.6 | [[6.3 Performance Metrics]] |
| `KS = max ∣CDF_负 − CDF_正∣` | 最大区分度：两条累计分布曲线之间的最大垂直距离 | 与 AUC 高度相关；风控常看 KS > 0.3（经验值） | [[6.3 Performance Metrics]] |
| PR-AUC（AUPRC） | PR 曲线下面积：正类极不平衡时比 ROC 更有信息量 | **baseline = 正类比例**，不是 0.5 | [[6.3 Performance Metrics]] |
| Lift / Cumulative Gains | Lift / 累计增益图：在 top k% 人群里能抓到多少事件，业务沟通最好用 | 业务沟通最好用的图 | [[6.3 Performance Metrics]]、[[6.4 Diagnostic & Visualization Tools]] |
| `RMSE = √(Σ(y−ŷ)²/n)`，`MAE = Σ∣y−ŷ∣/n` | 两个回归误差度量：一个先平方再开根、对极端值更敏感；一个大误差更稳 | RMSE 惩罚大误差，MAE 稳 | [[6.3 Performance Metrics]] |
| `R² = 1 − SS_res/SS_tot` | 解释方差比：1 减去残差平方和占总平方和的比例，样本外可以为负 | **可为负** | [[6.3 Performance Metrics]] |
| `MAPE = mean∣(y−ŷ)/y∣` | 平均绝对百分比误差：相对误差，用之前先确认 y 会不会接近 0 | **y≈0 时不可用**，且不惩罚大误差（经验限制） | [[6.3 Performance Metrics]] |
| `PSI = Σ (A_i − E_i)·ln(A_i/E_i)` | 群体稳定性指数：训练样本与当前样本的分箱分布差异，用来监控漂移 | <0.1 稳定 / 0.1–0.25 警戒 / >0.25 显著（经验值） | [[10. 保险风控考点总览]] |

---

## 6. 统计推断

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `p = P(数据至少这么极端 ∣ H₀ 真)` | p 值的定义：在 H₀ 为真的前提下，看到「至少这么极端」数据的概率 | **不是「H₀ 为真的概率」，不是效应大小** | [[1.7 Assumptions]] |
| 置信区间 `β̂ ± z·SE` | 置信区间：同时给出效应大小与不确定性，比单个 p 值信息更多 | 报告 CI 与效应量 | [[1.7 Assumptions]] |
| `SE(β̂) = σ/√n` | 系数的标准误：估计值的抽样波动，随样本量开根号下降 | n↑ → SE↓ | [[1.1 Ordinary Least Squares (OLS)]] |
| Bonferroni：`α/m` | Bonferroni 校正：把显著性门槛除以比较次数，最简单也最保守 | m 次比较时用 | [[6.1 Data Preparation & Validation]] |
| FDR（Benjamini–Hochberg） | FDR 控制：控制「判为显著的结果里假阳性占的比例」，比 Bonferroni 宽松 | 比 Bonferroni 宽松、power 更高 | [[6.1 Data Preparation & Validation]] |
| 嵌套 CV / 同折比较 | 比较多个模型时：要在同一折上比或做嵌套 CV，否则最优模型会被选择偏差抬高 | 否则最佳模型的 CV 表现被**虚高** | [[6.1 Data Preparation & Validation]] |
| `EPV = 少数类样本数 / 变量数` | 每个变量可用的少数类样本数：逻辑回归样本量的经验法则 | **10–20 EPV**（经验值） | [[1.6 Estimation Issues & Separation]] |
| Bootstrap SE / 百分位 CI | Bootstrap 标准误与百分位区间：不依赖分布假设的区间估计 | B 通常 ≥1000（经验值） | [[1.4 Model Fitting]] |

---

## 7. 共线性与数据质量

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `VIF_j = 1/(1−R²_j)` | VIF：第 j 个变量被其余变量解释的程度。注意它作用在**方差**上，**标准误只放大 √VIF 倍**（VIF=10 → SE ≈ 3.16 倍） | >5 关注、>10 严重（经验值） | [[3.4.2 Variance Inflation Factor (VIF)]] |
| VIF 只伤害**解释/推断** | 影响面：共线性伤的是**推断与解释**，训练范围内的预测基本无感（但结构漂移时预测会变脆、预测区间会变宽） | 目的决定处理方式 | [[3. Multicollinearity]] |
| 条件数 / 相关矩阵特征值 | 条件数（condition index）诊断：>10 中度、>30 严重，再配**方差分解比例**定位是哪几个变量 | 条件数 >30 提示问题（经验值） | [[3. Multicollinearity]] |
| `WOE_k = ln(dist_good_k / dist_bad_k)` | WOE：分箱的 log 似然比，把类别变量转成对 odds 近似线性的数值 | **先确认符号约定**；要求**单调** | [[2.3.1 WOE & IV]] |
| `IV = Σ (dist_good − dist_bad)·WOE` | IV：WOE 的加权和，用来看单个变量的预测力 | <0.02 无用 / 0.1–0.3 中 / 0.3–0.5 强 / >0.5 可疑（经验值） | [[2.3.1 WOE & IV]] |
| MCAR / MAR / MNAR | 三种缺失机制：决定「能不能插补」以及「插补结果可不可信」 | MAR 可插补（MICE），MNAR 要建模缺失本身 | [[4. Missing Data]] |
| 缺失率阈值 | 缺失率的经验线：超过一半缺失的列通常先考虑丢掉 | >50% 常考虑丢弃（经验值，看业务） | [[4. Missing Data]] |
| 评分刻度 `score = A − B·ln(odds)`，`B = PDO/ln 2`，`A = offset + B·ln(offset_odds)` | 评分刻度换算：把 log-odds 线性地折成业务分数 | 例：PDO=20 → B≈28.85；分数↑ 表示低风险 | [[2.3.1 WOE & IV]]、[[10. 保险风控考点总览]] |
| 不平衡处理 | 不平衡处理的四条路：重采样 / 类权重 / 阈值调整 / 换指标；**除截距外系数不变、排序指标不受影响**，且 SMOTE 必须在每折训练集内部做 | SMOTE 后**必须校准概率** | [[1.5 Dealing with Unbalanced Samples]] |

---

## 8. 其他速记（一句版）

| 主题 | 一句话 |
|---|---|
| 偏差-方差 | 总误差 = 偏差² + 方差 + 噪声；RF 靠平均降**方差**、Boosting 靠逐步修正降**偏差**（[[6.2 Model Diagnosis_Bias-Variance Tradeoff]]） |
| 特征选择三类 | 三类做法：filter 快、只看单变量；wrapper 准、但贵；embedded 在训练里做（Lasso、树）（[[5. Dimension Reduction]]） |
| 单变量筛选 | 单变量筛选常用：χ² / ANOVA / IV / 单变量 AUC，先各自看一遍再谈组合（[[5.2 Univariate Selection]]） |
| 多变量筛选 | 多变量筛选：VIF 查共线性、互信息查非线性依赖、RFE / Stepwise 做子集搜索（[[5.3 Multivariate Selection]]） |
| 降维 | 降维的代价：PCA 与 VARCLUS 换来的是不可解释的主成分 / 聚类（[[3.5.4 PCA]]、[[3.5.2 SAS VARCLUS]]） |
| 树 vs 线性模型 | 树与线性模型的差别：树无须标准化、能抓交互与非线性，但**不能外推**（[[7.2.2 Decision Tree]]） |

---


## 🗣️ 英文表达 / How to Say It

> 面试官说「walk me through the formula」时，你要**一边写一边念**。下面 35 条覆盖本卡的核心公式。
> 念法原则：**符号用英文名字**（β → "beta"，Σ → "the sum of"，√ → "the square root of"，∣ → "the absolute value of"）；
> **指数念 "e to the …"**；**括号里的分数念 "X over Y"**。术语见 [[06. 中英术语对照表]]。

### 1. 公式的英文读法（写下来 + 念出来）

| 公式 | 英文读法 |
|---|---|
| `ln(p/(1−p)) = β₀ + β₁x₁ + …` | "The log odds, or the logit of p, equals the log of p over one minus p, which equals beta-zero plus beta-one x-one, and so on." |
| `p = 1/(1+e^{−Xβ})` | "p equals one over one plus e to the minus X beta." |
| `OR_j = exp(β_j)` | "The odds ratio is e to the beta — exponentiating the coefficient gives the odds ratio." |
| `exp(β_j ± z·SE_j)` | "Exponentiate beta plus or minus z times the standard error, and that's the confidence interval for the odds ratio." |
| `μ = b′(θ)`，`Var(Y) = φ·b″(θ)` | "The mean is the first derivative of b, and the variance is phi times the second derivative of b." |
| `g(μ) = Xβ` | "The link applied to the mean equals X beta — that's the linear predictor." |
| `ℓ(β) = Σ[y·ln p + (1−y)·ln(1−p)]` | "The log-likelihood is the sum over observations of y log p plus one minus y times log of one minus p." |
| `D = 2[ℓ_sat − ℓ(β)]` | "Deviance is twice the saturated log-likelihood minus the fitted log-likelihood." |
| `AIC = −2ℓ + 2k`，`BIC = −2ℓ + k·ln n` | "AIC is minus two log-likelihood plus two k; BIC is minus two log-likelihood plus k times log n." |
| `ℓ(β) + ½·log det I(β)` | "The Firth penalty adds one half times the log determinant of the Fisher information matrix." |
| Ridge `+ λ·Σβ_j²` | "Minimise the residual sum of squares plus lambda times the sum of the squared coefficients." |
| Lasso `+ λ·Σ abs(β_j)` | "Plus lambda times the sum of the absolute values of the coefficients." |
| ElasticNet `λ[α·Σ abs(β_j) + (1−α)·Σβ_j²]` | "Lambda times alpha times the L1 norm plus one minus alpha times the L2 norm." |
| `H(t) = −Σ_k p_k·log₂ p_k` | "Entropy is minus the sum over classes of p log base two p." |
| `Gini(t) = 1 − Σ_k p_k²` | "Gini is one minus the sum of the squared class proportions." |
| `Err(t) = 1 − max_k p_k` | "The misclassification error is one minus the largest class proportion." |
| `Δ = H(parent) − Σ (n_child/n_parent)·H(child)` | "Information gain is the parent's entropy minus the weighted average of the children's entropies." |
| `R_α(T) = R(T) + α·n_T` | "The cost-complexity criterion is the tree's error plus alpha times the number of leaves." |
| `α_t = (R(t) − R(T_t))/(|T_t| − 1)` | "Alpha sub t is R of t minus R of T sub t, over the number of leaves in T sub t minus one." |
| `Var = ρσ² + (1−ρ)σ²/B` | "The variance is rho sigma squared plus one minus rho, times sigma squared over B." |
| `ε_t = Σ w_i·1[y_i≠h_t(x_i)] / Σ w_i` | "Epsilon sub t is the weighted error: the sum of the weights on the misclassified points, over the total weight." |
| `α_t = ½·ln((1−ε_t)/ε_t)` | "Alpha sub t is one half times the log of one minus epsilon, over epsilon." |
| `D_{t+1}(i) ∝ D_t(i)·exp(−α_t·y_i·h_t(x_i))` | "The next weight is proportional to the current weight times e to the minus alpha times y times h." |
| 上界 `∏_t 2√(ε_t(1−ε_t))` | "The training error is bounded by the product over t of two times the square root of epsilon times one minus epsilon." |
| `r_i = −∂L(y_i,f(x_i))/∂f(x_i)` | "The pseudo-residual is minus the derivative of the loss with respect to the current prediction." |
| `F_m(x) = F_{m−1}(x) + ν·h_m(x)` | "The new model is the previous model plus nu times the new tree — nu is the shrinkage, the learning rate." |
| `AUC = P(score_正 > score_负)` | "AUC is the probability that a randomly chosen positive scores higher than a randomly chosen negative." |
| `Gini = 2·AUC − 1` | "Gini is two times AUC minus one." |
| `KS = max ∣CDF_负 − CDF_正∣` | "KS is the maximum absolute gap between the two cumulative distributions." |
| `PSI = Σ (A_i − E_i)·ln(A_i/E_i)` | "PSI is the sum over bins of actual minus expected, times the log of actual over expected." |
| `VIF_j = 1/(1−R²_j)` | "VIF is one over one minus R-squared, where R-squared comes from regressing x-j on all the other predictors." |
| `WOE_k = ln(%Goods_k / %Bads_k)` | "WOE is the log of the percentage of goods over the percentage of bads — positive means lower risk." |
| `IV = Σ (%Goods − %Bads)·WOE` | "IV is the sum of the difference in the distributions times WOE." |
| `score = A − B·ln(odds)`，`B = PDO/ln 2` | "The score is A minus B times the log odds, and B equals PDO over the natural log of two." |
| `SE(β̂) = σ/√n` | "The standard error is sigma over the square root of n." |

### 2. 被追问时的两句话

> "Before I quote a number, let me flag that most of the thresholds we use here are rules of thumb, not theorems."
> "I can write the formula, or I can explain what it does — which would be more useful?"

## 相关入口

- 答题方法：[[03. 答题模板与追问应对]]
- 追问清单：[[05. 高频追问 TOP 30]]
- 学习路径：[[02. 三条学习路径]]
