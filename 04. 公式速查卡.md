---
title: 04. 公式速查卡
aliases: [公式速查, 公式卡, formula sheet]
tags: [sprint, 公式, 速查]
status: 完成
updated: 2026-09-16
---

# 04. 公式速查卡

> 一页纸，面试前 30 分钟只看这个。**所有「阈值」都是经验值**，不同机构口径不同，面试时说明「经验值是 X」即可。
> 列含义：`公式` | `含义` | `关键数字/阈值（经验值）` | `对应笔记`。

---

## 1. GLM 与逻辑回归

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `ln(p/(1−p)) = β₀ + β₁x₁ + …` | logit（log-odds）线性模型，逻辑回归的核心 | β 的尺度：每单位 x 改变 log-odds | [[1.3 Introducing Logistic Regression]] |
| `p = 1/(1+e^{−Xβ})` | sigmoid 把线性预测 η 压到 (0,1) | η=0 → p=0.5 | [[1.3 Introducing Logistic Regression]] |
| `OR_j = exp(β_j)` | x_j 增 1 单位时的 odds 倍数 | OR=1 无效应；0.5/2 是等量级 | [[1.3 Introducing Logistic Regression]] |
| `exp(β_j ± z_{1−α/2}·SE_j)` | odds ratio 的置信区间 | 用 Wald CI（大样本） | [[1.3 Introducing Logistic Regression]] |
| `μ = b′(θ)`，`Var(Y) = φ·b″(θ)` | 指数族：均值 = 累积函数一阶导 | 正态 / 二项 / Poisson / Gamma 均属此族 | [[1.2 Generalized Linear Models (GLMs)]] |
| `g(μ) = Xβ` | link 函数把均值连到线性预测 | 二项→logit，Poisson→log，Gamma→inverse | [[1.2 Generalized Linear Models (GLMs)]] |
| `ℓ(β)=Σ[y·ln p+(1−y)·ln(1−p)]` | 对数似然（= 负交叉熵） | 用 MLE 而非最小二乘 | [[1.4 Model Fitting]] |
| `D = 2[ℓ_sat − ℓ(β)]` | deviance（偏差），模型拟合的损失度量 | 嵌套模型比较用；近似 χ² | [[1.2 Generalized Linear Models (GLMs)]] |
| `AIC = −2ℓ + 2k`，`BIC = −2ℓ + k·ln n` | 复杂度惩罚的信息准则 | BIC 罚更重（k·ln n） | [[1.6.3 Regularization]] |
| `ℓ(β) + ½·log det I(β)` | **Firth 惩罚**（Jeffreys prior） | 完全分离时给**有限估计**，R 的 `logistf` | [[1.6 Estimation Issues & Separation]] |
| 分离时 `β → ∞`，对数似然 → 0 | 完全/准分离：MLE 不存在（`ℓ` 逼近 0 但取不到） | Ridge/Lasso 只缓解，CV 常选 λ=0；Firth 给有限估计 | [[1.6 Estimation Issues & Separation]] |
| 回顾性/病例对照抽样：除截距外系数不变 | 逻辑回归对**给定 X 的 Y 抽样**免疫 | 截距可用 offset 校正 | [[1.6 Estimation Issues & Separation]] |
| 不用 `(y−η)²`（L2 损失） | 平方损失在概率尺度上**梯度太平、尺度太小**（不是「有偏」） | 所以用交叉熵 / deviance | [[1.4 Model Fitting]] |

---

## 2. 正则化

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `Ridge: min ‖y−Xβ‖² + λ·Σβ_j²` | L2：系数收缩，不置零 | λ↑ → β↓；需标准化 | [[1.6.3 Regularization]] |
| `Lasso: min ‖y−Xβ‖² + λ·Σ abs(β_j)` | L1：产生稀疏解（变量选择） | λ 大到一定值全部置 0 | [[1.6.3 Regularization]] |
| `ElasticNet: min … + λ[α·Σ abs(β_j) + (1−α)·Σβ_j²]` | L1+L2 混合 | α=1 → Lasso，α=0 → Ridge | [[1.6.3 Regularization]] |
| `CV 选 λ`：λ_min 或 **1-SE 规则** | 交叉验证选惩罚强度 | 1-SE：选最简模型，误差在 1 个 SE 内 | [[1.6.3 Regularization]]、[[6.1 Data Preparation & Validation]] |
| 标准化必要性 | 惩罚项对尺度敏感 | 先 `(x−μ)/σ`；哑变量通常不罚 | [[1.6.3 Regularization]] |
| 正则化 ≠ 解决分离 | 惩罚是**缓解**；分离下 CV 常选 λ=0 | 要**有限估计**用 Firth | [[1.6 Estimation Issues & Separation]] |

---

## 3. 树与不纯度

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `H(t) = −Σ_k p_k·log₂ p_k` | 熵（不纯度） | 二分类最大 1 bit（p=0.5）；纯节点 0 | [[7.2.2 Decision Tree]] |
| `Gini(t) = 1 − Σ_k p_k²` | Gini 不纯度 | 二分类最大 0.5（p=0.5） | [[7.5 CART Algorithm]] |
| `Err(t) = 1 − max_k p_k` | 误分类率 | 二分类最大 0.5；**分段线性** | [[7.2.2 Decision Tree]] |
| 熵/Gini 严格凹 → 分裂增益恒 > 0 | 即使两个子节点同样纯，增益也 > 0 | **建树用熵/Gini** | [[7.2.2 Decision Tree]] |
| 误分类率**分段线性**：两子节点同在 p=0.5 一侧 → 增益 = 0 | 无法区分无改进的分裂 | **剪枝可用误分类率** | [[7.2.2.4. Pruning Decision Trees]] |
| `Δ = H(parent) − Σ (n_child/n_parent)·H(child)` | 信息增益（ID3 准则） | 偏好多值特征 → C4.5 修正 | [[7.2.2.3.1 ID3 Algorithm]] |
| `GainRatio = Δ / SplitInfo` | 增益率（C4.5），惩罚取值多的特征 | SplitInfo = 分裂自身的熵 | [[7.2.3.3 C4.5 Algorithm]] |
| `ΔI_t(j) = I(S_t) − (n_L/n_t)·I(S_L) − (n_R/n_t)·I(S_R)` | 杂质下降（MDI 的基础量） | 累加后即 MDI 重要性 | [[7.4. Quantifying Feature Importance]] |
| `R_α(T) = R(T) + α·n_T` | **代价复杂度（CCP）** | α=0 → 大树；α↑ → 更小树 | [[7.2.2.4. Pruning Decision Trees]] |
| weakest-link：每步剪掉使 `R_α` 上升最小的子树 | 生成 α 的嵌套路径 | 用 **K 折 CV 选 α**，可选 **1-SE 原则** | [[7.2.2.4. Pruning Decision Trees]]、[[7.5 CART Algorithm]] |
| 树**不需要**标准化 | 分裂只看排序/阈值 | 单调变换不变 | [[7.2.2 Decision Tree]] |

---

## 4. 集成与 Boosting

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `Var = ρσ² + (1−ρ)σ²/B` | Bagging 后的方差 | 关键降 **ρ**（树间相关）；B↑ 第二项 → 0 | [[8.3. Bagging (Bootstrap Aggregating)]] |
| `P(样本不在 bootstrap 中) = (1−1/n)^n → e^{−1} ≈ 0.368` | 约 **36.8%** 为 OOB，**63.2%** 被抽到 | OOB 当验证集用 | [[8.3. Bagging (Bootstrap Aggregating)]] |
| RF 的 `m`：分类 `√p`，回归 `p/3` | 每次分裂随机抽的特征数 | m↓ → 树更独立、偏差略升 | [[7.2. Building the Forest]] |
| MDI（RSS gain） vs permutation（在 **OOB** 上置换） | 两种特征重要性 | 高基数变量在 MDI 被**夸大** | [[7.4. Quantifying Feature Importance]] |
| AdaBoost 加权误差 `ε_t = Σ w_i·1[y_i≠h_t(x_i)] / Σ w_i` | 第 t 轮的加权错误率 | 要求弱学习器 ε_t < 0.5 | [[8.4.2.1 AdaBoost (Adaptive Boosting)]] |
| `α_t = ½·ln((1−ε_t)/ε_t)` | 该学习器的投票权重 | ε_t→0 → α_t 大；**ε_t>0.5 → α_t<0** | [[8.4.2.1.4 AdaBoost Formulas]] |
| `D_{t+1}(i) ∝ D_t(i)·exp(−α_t·y_i·h_t(x_i))`，归一化 `/Z_t` | 样本权重更新 | 错分样本权重上升 | [[8.4.2.1 AdaBoost (Adaptive Boosting)]] |
| 训练误差上界 `∏_t 2√(ε_t(1−ε_t)) < 1` | 理论上界随轮数下降 | **实际 0/1 误差不保证单调下降** | [[8.4.2.1.4 AdaBoost Formulas]] |
| AdaBoost = **指数损失**下的前向分步加性模型 | 与 GBM 的统一视角 | 损失 `exp(−y·f(x))` | [[8.4. Boosting]]、[[8.6 Bagging VS Boosting]] |
| GBM：`r_i = −[∂L(y_i,f(x_i))/∂f(x_i)]` | **伪残差** = 负梯度 | 平方损失时即普通残差 | [[8.4.2.2 Gradient Boosting]] |
| `F_m(x) = F_{m−1}(x) + ν·h_m(x)` | 加性模型 + shrinkage | ν = eta，步长 | [[8.4.2.2 Gradient Boosting]] |
| eta 与树数 T **反比** | 调参核心 | eta 常见 0.01–0.1；树要**浅**；加子采样 | [[8.4.2.2 Gradient Boosting]] |
| RF 调参少 / GBM 调好更强 | 选型结论 | RF 并行、鲁棒；GBM 串行、精度高 | [[8.6 Bagging VS Boosting]] |

---

## 5. 评估指标

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `Precision = TP/(TP+FP)`，`Recall = TP/(TP+FN)` | 精确率 / 召回率 | 阈值相关，二选一要看业务代价 | [[6.3 Performance Metrics]] |
| `F1 = 2PR/(P+R)` | 精确率与召回的调和平均 | 不平衡时比 accuracy 有意义 | [[6.3 Performance Metrics]] |
| `Accuracy = (TP+TN)/N` | 整体正确率 | **不平衡数据下会骗人** | [[6.3 Performance Metrics]] |
| ROC 曲线 / `AUC = P(score_正 > score_负)` | 排序能力，阈值无关 | 0.5=随机；0.7–0.8 可接受（经验值，看场景） | [[6.3 Performance Metrics]] |
| `Gini = 2·AUC − 1` | 与 AUC 的换算 | AUC 0.8 → Gini 0.6 | [[6.3 Performance Metrics]] |
| `KS = max ∣CDF_负 − CDF_正∣` | 最大区分度 | 与 AUC 高度相关；风控常看 KS > 0.3（经验值） | [[6.3 Performance Metrics]] |
| PR-AUC（AUPRC） | 正类极不平衡时的排序度量 | **baseline = 正类比例**，不是 0.5 | [[6.3 Performance Metrics]] |
| Lift / Cumulative Gains | 前 k% 里的捕获率 | 业务沟通最好用的图 | [[6.3 Performance Metrics]]、[[6.4 Diagnostic & Visualization Tools]] |
| `RMSE = √(Σ(y−ŷ)²/n)`，`MAE = Σ∣y−ŷ∣/n` | 回归误差 | RMSE 惩罚大误差，MAE 稳 | [[6.3 Performance Metrics]] |
| `R² = 1 − SS_res/SS_tot` | 解释方差比 | **可为负** | [[6.3 Performance Metrics]] |
| `MAPE = mean∣(y−ŷ)/y∣` | 相对误差 | **y≈0 时不可用**，且不惩罚大误差（经验限制） | [[6.3 Performance Metrics]] |
| `PSI = Σ (A_i − E_i)·ln(A_i/E_i)` | 分布稳定性（漂移） | <0.1 稳定 / 0.1–0.25 警戒 / >0.25 显著（经验值） | [[10. 保险风控考点总览]] |

---

## 6. 统计推断

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `p = P(数据至少这么极端 ∣ H₀ 真)` | p 值定义 | **不是「H₀ 为真的概率」，不是效应大小** | [[1.7 Assumptions]] |
| 置信区间 `β̂ ± z·SE` | 比 p 值信息更多 | 报告 CI 与效应量 | [[1.7 Assumptions]] |
| `SE(β̂) = σ/√n` | 估计的抽样波动 | n↑ → SE↓ | [[1.1 Ordinary Least Squares (OLS)]] |
| Bonferroni：`α/m` | 多重比较校正（保守） | m 次比较时用 | [[6.1 Data Preparation & Validation]] |
| FDR（Benjamini–Hochberg） | 控制错误发现比例 | 比 Bonferroni 宽松、power 更高 | [[6.1 Data Preparation & Validation]] |
| 嵌套 CV / 同折比较 | 比较多个模型时避免选择偏差 | 否则最佳模型的 CV 表现被**虚高** | [[6.1 Data Preparation & Validation]] |
| `EPV = 少数类样本数 / 变量数` | 逻辑回归样本量经验法则 | **10–20 EPV**（经验值） | [[1.6 Estimation Issues & Separation]] |
| Bootstrap SE / 百分位 CI | 非参数区间估计 | B 通常 ≥1000（经验值） | [[1.4 Model Fitting]] |

---

## 7. 共线性与数据质量

| 公式 | 含义 | 关键数字/阈值 | 对应笔记 |
|---|---|---|---|
| `VIF_j = 1/(1−R²_j)` | 第 j 个变量被其他变量解释的程度 | >5 关注、>10 严重（经验值） | [[3.4.2 Variance Inflation Factor (VIF)]] |
| VIF 只伤害**解释/推断** | 预测与树模型基本无感 | 目的决定处理方式 | [[3. Multicollinearity]] |
| 条件数 / 相关矩阵特征值 | 共线性诊断 | 条件数 >30 提示问题（经验值） | [[3. Multicollinearity]] |
| `WOE_k = ln(dist_good_k / dist_bad_k)` | 分箱的 log 似然比 | **先确认符号约定**；要求**单调** | [[2.3.1 WOE & IV]] |
| `IV = Σ (dist_good − dist_bad)·WOE` | 单变量预测力 | <0.02 无用 / 0.1–0.3 中 / 0.3–0.5 强 / >0.5 可疑（经验值） | [[2.3.1 WOE & IV]] |
| MCAR / MAR / MNAR | 缺失机制 | MAR 可插补（MICE），MNAR 要建模缺失本身 | [[4. Missing Data]] |
| 缺失率阈值 | 删列经验线 | >50% 常考虑丢弃（经验值，看业务） | [[4. Missing Data]] |
| 评分刻度 `score = A − B·ln(odds)`，`B = PDO/ln 2`，`A = offset + B·ln(offset_odds)` | 把 log-odds 折成业务分数 | 例：PDO=20 → B≈28.85；分数↑ 表示低风险 | [[2.3.1 WOE & IV]]、[[10. 保险风控考点总览]] |
| 不平衡处理 | 重采样 / 权重 / 阈值调整 | SMOTE 后**必须校准概率** | [[1.5 Dealing with Unbalanced Samples]] |

---

## 8. 其他速记（一句版）

| 主题 | 一句话 |
|---|---|
| 偏差-方差 | 总误差 = 偏差² + 方差 + 噪声；RF 降方差、Boosting 降偏差（[[6.2 Model Diagnosis_Bias-Variance Tradeoff]]） |
| 特征选择三类 | filter（快、单变量）/ wrapper（准、贵）/ embedded（Lasso、树）（[[5. Dimension Reduction]]） |
| 单变量筛选 | χ² / ANOVA / IV / 单变量 AUC（[[5.2 Univariate Selection]]） |
| 多变量筛选 | VIF / 互信息 / RFE / Stepwise（[[5.3 Multivariate Selection]]） |
| 降维 | PCA 与 VARCLUS 换来不可解释性（[[3.5.4 PCA]]、[[3.5.2 SAS VARCLUS]]） |
| 树 vs 线性模型 | 树无须标准化、能抓交互与非线性，但**不能外推**（[[7.2.2 Decision Tree]]） |

---

## 相关入口

- 答题方法：[[03. 答题模板与追问应对]]
- 追问清单：[[05. 高频追问 TOP 30]]
- 学习路径：[[02. 三条学习路径]]
