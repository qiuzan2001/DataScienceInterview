---
title: "定义原文库"
aliases: [标准定义, Definitions, Definition Sourcebook]
tags: [meta, definitions, sources, ssot]
priority: "⭐⭐⭐"
status: 完成
updated: 2026-09-16
---

# 定义原文库

> **本文件是全库「定义」的单一事实来源。**
> 凡笔记里出现定义，一律以本文的**教材英文原文**为准；中文只作解释。
>
> **为什么要有这个文件**：面试官问「什么是 X」时，能背出**教材的标准措辞**和**标准数学记法**，与只能用自己的话描述，是两种水平。
>
> **来源**（均为公开可访问的高质量教材）：
> - **PSL** — Practical Statistical Learning, UIUC，基于 *The Elements of Statistical Learning*（Hastie/Tibshirani/Friedman）。<https://liangfgithub.github.io/PSL/>
> - **LDA** — Loss Data Analytics（开源精算教材）。<https://openacttexts.github.io/Loss-Data-Analytics/>
>
> **引用规则**：本文所有英文均为**逐字引用**，出处附章节与 URL；数学公式由网页 LaTeX 转写为 `$...$`，符号未改。

> [!note] 关于 PSL 的页面结构
> PSL 的 `w1_1_index.html`、`w1_2_index.html` 是**只有目录、没有正文**的章节落地页（实测抓取只有导航栏）。因此 A1/A2 的原文取自同一节的正文子页 `w1_1_type_of_learning.html`、`w1_2_intro_LS_kNN.html`、`w1_2_discussion.html`，URL 逐条列在下方。

## 目录

**A. 统计学习与线性模型**
- [[#A1. 统计学习 / 监督学习|A1. 统计学习 / 监督学习]]
- [[#A2. 最小二乘 vs k-NN（参数 vs 非参数）|A2. 最小二乘 vs k-NN（参数 vs 非参数）]]
- [[#A3. 多元线性回归 / OLS 估计量|A3. 多元线性回归 / OLS 估计量]]
- [[#A4. 线性回归的几何解释（投影）|A4. 线性回归的几何解释（投影）]]
- [[#A5. 共线性与线性回归的实践问题|A5. 共线性与线性回归的实践问题]]

**B. 变量选择与正则化**
- [[#B6. 子集选择（best subset / forward / backward / stepwise）|B6. 子集选择（best subset / forward / backward / stepwise）]]
- [[#B7. 正则化 / 偏差-方差视角|B7. 正则化 / 偏差-方差视角]]
- [[#B8. 岭回归（ridge regression）|B8. 岭回归（ridge regression）]]
- [[#B9. Lasso 与稀疏性|B9. Lasso 与稀疏性]]
- [[#B10. Ridge vs Lasso 的取舍|B10. Ridge vs Lasso 的取舍]]

**C. 树与集成**
- [[#C11. 回归树 / 递归二分|C11. 回归树 / 递归二分]]
- [[#C12. 代价复杂度剪枝（CCP）/ weakest link / 1-SE 原则|C12. 代价复杂度剪枝（CCP）/ weakest link / 1-SE 原则]]
- [[#C13. 随机森林 / m 的推荐值 / OOB / 两种重要性|C13. 随机森林 / m 的推荐值 / OOB / 两种重要性]]
- [[#C14. GBM / 学习率 eta / 调参要点|C14. GBM / 学习率 eta / 调参要点]]
- [[#C15. 分类树的不纯度（entropy / Gini / misclassification）|C15. 分类树的不纯度（entropy / Gini / misclassification）]]
- [[#C16. 误分类率 vs 熵（严格凹 vs 分段线性）|C16. 误分类率 vs 熵（严格凹 vs 分段线性）]]
- [[#C17. AdaBoost 算法与训练误差上界|C17. AdaBoost 算法与训练误差上界]]
- [[#C18. 前向分步加性模型（forward stagewise additive modeling）|C18. 前向分步加性模型（forward stagewise additive modeling）]]

**D. 非线性与判别**
- [[#D19. 多项式回归|D19. 多项式回归]]
- [[#D20. 样条（回归样条 / 自然三次样条）|D20. 样条（回归样条 / 自然三次样条）]]
- [[#D21. 平滑样条与 GCV|D21. 平滑样条与 GCV]]
- [[#D22. 判别分析 / LDA / QDA|D22. 判别分析 / LDA / QDA]]
- [[#D23. 朴素贝叶斯分类器|D23. 朴素贝叶斯分类器]]

**E. 逻辑回归**
- [[#E24. 逻辑回归的设定（setup）与 logit link|E24. 逻辑回归的设定（setup）与 logit link]]
- [[#E25. MLE（为什么用似然而不是 L2 损失）|E25. MLE（为什么用似然而不是 L2 损失）]]
- [[#E26. 分离数据（separable data）与正则化能否解决|E26. 分离数据（separable data）与正则化能否解决]]
- [[#E27. 回顾性抽样（retrospective / case-control sampling）|E27. 回顾性抽样（retrospective / case-control sampling）]]

**F. 保险 / 精算（LDA）**
- [[#F28. GLM 框架|F28. GLM 框架]]
- [[#F29. Poisson 回归与 offset（exposure）|F29. Poisson 回归与 offset（exposure）]]
- [[#F30. 乘性费率 / relativity（相对数）|F30. 乘性费率 / relativity（相对数）]]
- [[#F31. Tweedie 分布（复合 Poisson–Gamma）|F31. Tweedie 分布（复合 Poisson–Gamma）]]
- [[#F32. 严重度分布（Gamma / Pareto / Weibull）|F32. 严重度分布（Gamma / Pareto / Weibull）]]
- [[#F33. 频率分布（Poisson / 负二项 / 二项）|F33. 频率分布（Poisson / 负二项 / 二项）]]

---

## A. 统计学习与线性模型

### A1. 统计学习 / 监督学习

**教材原文 · PSL §1.1.1 Types of statistical learning problems（页面 w1_1_type_of_learning.html）**
（<https://liangfgithub.github.io/PSL/w1/w1_1_type_of_learning.html>）

> "Supervised learning is about solving problems where we have a target variable, denoted as Y, and a set of features or covariates, often represented as X, which are multidimensional. Our goal is to build a predictive model or a “black box” that can ingest these features and produce predictions for Y."
>
> "Supervised learning aims to predict a target variable (Y) using features (X)."
>
> "Unsupervised learning involves analyzing data without a specific target prediction. Instead, we aim to uncover hidden structures, like clusters or associations, within the data."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 训练集 $(x_i, y_i)_{i=1}^n$，$x_i \in \mathbb{R}^p$；回归 $y_i \in \mathbb{R}$，分类 $y_i \in \{0, 1\}$（或 $\{1, \dots, K\}$）。
- 学习目标（经验风险最小化）$\min_f \frac{1}{n} \sum_{i=1}^n L(f(x_i), y_i)$；分类下常用 0-1 损失 $L(f(x), y) = 0$ if $y = f(x)$，否则 $1$。
- （上述两行是通用形式，非某本教材独有；PSL §9.1.1 用的是同一记法。）

**中文解释**

监督学习＝有标签 $Y$，用特征 $X$ 预测 $Y$；$Y$ 数值型是回归，$Y$ 分类型是分类。无监督学习没有目标变量，只从 $X$ 里找结构（聚类、关联）。半监督介于两者之间，用少量标签 + 大量无标签数据。

**面试怎么用**

被问「什么是监督学习」时，先说 "problems where we have a target variable Y and a set of features X"，再补 regression/classification 的分岔，措辞直接来自教材。

---

### A2. 最小二乘 vs k-NN（参数 vs 非参数）

**教材原文 · PSL §1.2.1 Introduction to LS and kNN（含 §1.2.4 Discussion）**
（<https://liangfgithub.github.io/PSL/w1/w1_2_intro_LS_kNN.html>）

> "The k-Nearest Neighbor (kNN) algorithm is a remarkably simple approach that requires no training. When presented with a test point x, we identify the top k samples in the training data that are closest (in terms of x) to this test point. For regression tasks, we simply output the average of the Y values corresponding to these top k neighbors."
>
> "When discussing model complexity, we often refer to the number of parameters or the effective number of degrees of freedom (DF) in the model. For parametric models like linear regression, this is straightforward: it’s the number of coefficients, p+1, if the feature vector X is p-dimensional."
>
> "But kNN is a non-parametric method, so things are a bit different. The complexity of kNN is inversely related to k and can be approximated as the ratio n/k."
>
> "On the other hand, kNN assumes only local smoothness, implying nearby points should yield similar responses. It generally has low bias and can fit many functions, making it consistent."

**标准记法**（通用形式，非某本教材独有，除非注明）

- kNN 预测（回归）$\hat{f}(x) = \frac{1}{k} \sum_{x_i \in N_k(x)} y_i$，其中 $N_k(x)$ 是离 $x$ 最近的 $k$ 个训练点；分类用多数投票或邻域类频率。
- 参数方法的模型复杂度 = 参数个数（线性回归为 $p+1$）；非参数方法的有效自由度 $\approx n/k$。
- （通用形式，非某本教材独有。）

**中文解释**

线性回归是参数方法：只估 $p+1$ 个系数，模型复杂度是个固定数字，但线性假设不成立时偏差大。k-NN 是非参数方法：不训练、把最近的 $k$ 个点平均（或投票），偏差小但方差大，复杂度与 $k$ 反向（$\approx n/k$）。两者是「强假设低方差」对「弱假设高方差」的教科书对照。

**面试怎么用**

被问「参数 vs 非参数」或「模型复杂度怎么衡量」时，直接给 "number of coefficients, p+1" 与 "the ratio n/k" 这组对照。

---

### A3. 多元线性回归 / OLS 估计量

**教材原文 · PSL §2.1.1 Setup、§2.1.3 LS Estimate（页面 w2_1.html）**
（<https://liangfgithub.github.io/PSL/w2/w2_1.html>）

> "The least square method is a standard technique for estimating parameters in regression models. It is used to obtain the least square estimates of the beta coefficients by minimizing a quantity known as the residual sum of squares, or RSS for short."
>
> "In statistics, placing a hat on a symbol indicates it’s an estimate. Thus, $\hat{\boldsymbol{\beta}}$ , is an estimate of the true coefficient vector based on a collection of n samples."
>
> "The rationale for using n-p-1 as the denominator is that the degree of freedom of the n residuals is n-p-1."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $\text{RSS}(\boldsymbol{\beta}) = \sum_{i=1}^n (y_i - \beta_0 - x_{i1} \beta_1 - \cdots - x_{ip} \beta_p)^2 = \| \mathbf{y} - \mathbf{X} \boldsymbol{\beta} \|^2$
- OLS 估计 $\hat{\boldsymbol{\beta}} = (\mathbf{X}^t \mathbf{X})^{-1} \mathbf{X}^t \mathbf{y}$（假设 X 满秩）；$\hat{\sigma}^2 = \text{RSS}/(n-p-1)$。
- （通用形式，非某本教材独有；矩阵写法与 PSL §2.1.3 一致。）

**中文解释**

多元线性回归把 $Y$ 写成 $X$ 的线性函数加误差；OLS 就是最小化残差平方和 RSS 得到 $\hat{\beta}$。分母用 $n-p-1$ 是因为 $n$ 个残差被 $p+1$ 个正规方程约束，自由度只剩 $n-p-1$。

**面试怎么用**

「OLS 是什么」永远是 "minimizing a quantity known as the residual sum of squares"，顺口带上 $\hat\sigma^2$ 的分母为什么是 $n-p-1$。

---

### A4. 线性回归的几何解释（投影）

**教材原文 · PSL §2.2.1–§2.2.3（页面 w2_2.html）**
（<https://liangfgithub.github.io/PSL/w2/w2_2.html>）

> "For regression analysis, we often deal with a special type of linear subspace known as the column space of the design matrix. Recall the p+1 columns of X, each being a vector in $\mathbb{R}^n$ . The column space, denoted by $C(X) = \{ \mathbf{X} \boldsymbol{\beta}, \boldsymbol{\beta} \in \mathbb{R}^{p+1} \}$ contains vectors that can be expressed as a linear combination of the columns of X. In other words, any vector in C(X) can be represented as X times a coefficient vector."
>
> "The least squares optimization problem is equivalent to finding a vector v on this plane so that the difference vector, stretching from y to v, attains the smallest norm. Intuitively, the optimal v is the projection of y onto the space C(X), the plane containing the yellow triangle."
>
> "This reveals the essence of least squares: it decomposes the n-dimensional data vector y into two orthogonal components:"

**标准记法**（通用形式，非某本教材独有，除非注明）

- 列空间 $C(X) = \{ \mathbf{X} \boldsymbol{\beta}, \boldsymbol{\beta} \in \mathbb{R}^{p+1} \}$；$\hat{\mathbf{y}} = \mathbf{X}\hat{\boldsymbol{\beta}} = H\mathbf{y}$，$H = \mathbf{X}(\mathbf{X}^t\mathbf{X})^{-1}\mathbf{X}^t$（投影/帽子矩阵）。
- 正交分解 $\mathbf{y} = \hat{\mathbf{y}} + \mathbf{r}$，$\hat{\mathbf{y}}^t \mathbf{r} = 0$；勾股定理 $\| \mathbf{y} - \bar{\mathbf{y}} \|^2 = \| \hat{\mathbf{y}} - \bar{\mathbf{y}} \|^2 + \| \mathbf{r}\|^2$。
- $R^2 = \frac{\| \hat{\mathbf{y}} - \bar{\mathbf{y}} \|^2}{\| \mathbf{y} - \bar{\mathbf{y}} \|^2} = 1 - \frac{\text{RSS}}{\text{TSS}}$。
- （通用形式，非某本教材独有。）

**中文解释**

OLS 的解就是把数据向量 $\mathbf{y}$ 正交投影到设计矩阵的列空间 $C(X)$ 上：$\hat{\mathbf{y}}$ 是投影，残差 $\mathbf{r}$ 与列空间正交。由此得到勾股分解，也解释了 $R^2$ 是「被解释的方差比例」。

**面试怎么用**

被问「为什么残差和 $X$ 正交」「$R^2$ 怎么来的」时，用投影 + 勾股定理一次讲完，这是教材原话 "the projection of y onto the space C(X)"。

---

### A5. 共线性与线性回归的实践问题

**教材原文 · PSL §2.3.5 Collinearity（页面 w2_3.html）**
（<https://liangfgithub.github.io/PSL/w2/w2_3.html>）

> "Collinearity arises when predictors in a regression model are closely related or have high correlations with one another. In this case, the contribution of a particular predictor could be masked by other predictors, which create difficulties for statistical inference on the coefficients beta."
>
> "Often results in a high R-square and a significant F-test, but insignificant p-values for individual predictors."
>
> "On the other hand, if your primary aim is prediction, collinearity poses fewer concerns."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 共线性（近似共线）$X_j \approx \sum_{k \ne j} c_k X_k$；完全共线即 $\text{rank}(\mathbf{X}) < p+1$，$\mathbf{X}^t\mathbf{X}$ 不可逆。
- 诊断常用方差膨胀因子 $\text{VIF}_j = \frac{1}{1 - R_j^2}$，$R_j^2$ 为 $X_j$ 对其他预测变量回归的 $R^2$。
- $\text{Var}(\hat{\beta}_j) = \frac{\sigma^2}{(n-1)\,\text{Var}(X_j)} \cdot \text{VIF}_j$：方差被放大，系数的标准误膨胀。
- （通用形式，非某本教材独有；VIF 与方差膨胀公式为教科书通行记法，PSL 本节只作定性描述。）

**中文解释**

共线性指预测变量之间高度相关，某个变量的贡献会被邻居「抢走」。典型症状是整体 $R^2$ 高、F 检验显著，但单个系数都不显著、符号还可能翻转。要解释系数就必须处理（删变量、合并变量如 PCA）；只要预测，则可以不管。

**面试怎么用**

面试标准答案的三段式：现象（high R-square + significant F + insignificant p-values）→ 原因（contribution masked by other predictors）→ 处方（解释就处理、预测就放过）。

---

## B. 变量选择与正则化

### B6. 子集选择（best subset / forward / backward / stepwise）

**教材原文 · PSL §3.1.2 Selection Criteria、§3.1.4 Search Algorithms（页面 w3_1_subset.html）**
（<https://liangfgithub.github.io/PSL/w3/w3_1_subset.html>）

> "The score for model selection typically comprises two components: A goodness of fit measure: often an increasing function of the residual sum of squares. A complexity penalty term: often an increasing function of p, the number of non-intercepted variables."
>
> "A popular method is the ‘level-wise search algorithm’, which works as follows: Grouping Models by Size : Imagine there are p potential predictors. Models can then be grouped into p groups. For instance: The first group consists of models with just one predictor, the second group has models with two predictors, and so on, until the last group, which contains a model with all predictors."
>
> "Backward Search : Start with the full model and sequentially eliminate predictors until the score no longer improves."
>
> "Forward Search : Start with a null model (only the intercept) and sequentially add predictors until the score no longer improves."
>
> "The nature of greedy algorithms, given their specific path of search, means they may stop at a locally optimal solution rather than a globally optimal one. However, they’re faster and often yield solutions that are practically sufficient."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 模型由二元向量索引，$2^p$ 个候选；打分 = 拟合优度 + 复杂度惩罚：minimize $\text{RSS} + \lambda |\mathcal{M}|$。
- 常见准则（通用形式）：$\text{AIC} = n \log(\text{RSS}/n) + 2|\mathcal{M}|$；$\text{BIC} = n \log(\text{RSS}/n) + \log(n)\,|\mathcal{M}|$；Mallow's $C_p = \text{RSS}/\hat{\sigma}^2 + 2|\mathcal{M}| - n$。
- （通用形式，非某本教材独有；PSL §3.1.2 只写出「拟合项 + 惩罚项」的结构并点名 Cp/AIC/BIC。）

**中文解释**

子集选择＝在 $2^p$ 个候选模型里按「拟合好 + 惩罚复杂度」打分找最优。全枚举（level-wise）在 $p>40$ 基本不可行，于是用贪心：backward 从全模型删、forward 从截距模型加、stepwise 两者结合且允许反悔。贪心只能到局部最优，但快且够用。

**面试怎么用**

被问「变量选择怎么做」时，先说打分结构（goodness of fit + complexity penalty），再列三种贪心搜索及其局限（local optimum）。

---

### B7. 正则化 / 偏差-方差视角

**教材原文 · PSL §3.2 Regularization（页面 w3_2_regularize.html）**
（<https://liangfgithub.github.io/PSL/w3/w3_2_regularize.html>）

> "In this lecture, we’ll delve into variable selection using the regularization framework. Previously, we discussed using AIC or BIC for variable subset selection. This process can be framed as the pursuit of minimizing a specific objective function. Assuming we know or can reliably estimate the error variance, sigma-square, the objective function becomes:"
>
> "… counts the number of predictors in our model. Both AIC and BIC essentially correspond to different choices of lambda."
>
> "Next, we’ll introduce two alternative regularization or penalty terms for beta. Ridge regression employs the L2 penalty on beta, while Lasso regression uses the L1 penalty."
>
> "The degree of shrinkage varies with the magnitude of singular values, shrinking less for larger singular values and more for smaller ones. This accounts for the regularization effect that makes ridge regression robust, especially when X has multicollinearity or is not full-rank."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $\min_{\boldsymbol{\beta}} \| \mathbf{y} - \mathbf{X} \boldsymbol{\beta}\|^2 + \lambda \| \boldsymbol{\beta} \|_0$，其中 $\| \boldsymbol{\beta} \|_0 = \sum_{j=1}^p \mathbf{1}_{\{ \beta_j \ne 0 \}}$；$L_0$ 惩罚即 AIC/BIC 的连续化版本。
- Ridge：$\min \| \mathbf{y} - \mathbf{X} \boldsymbol{\beta}\|^2 + \lambda \| \boldsymbol{\beta} \|^2$；Lasso：$\min \| \mathbf{y} - \mathbf{X} \boldsymbol{\beta}\|^2 + \lambda \| \boldsymbol{\beta} \|_1$。
- （通用形式，非某本教材独有；两行目标函数即 PSL §3.2 原文。）

**中文解释**

正则化把「变量选择」写成一个惩罚目标函数：$L_0$ 惩罚数非零系数（等于 AIC/BIC 家族），$L_2$（ridge）压缩系数，$L_1$（lasso）压缩并置零。惩罚项把偏差换方差，所以能降低测试误差。注意 $L_1/L_2$ 对尺度敏感，必须先中心化/标准化，且不罚截距。

**面试怎么用**

一句话把 AIC/BIC 与 ridge/lasso 串成一个统一框架：都是 "minimizing a specific objective function"，只是惩罚不同。

---

### B8. 岭回归（ridge regression）

**教材原文 · PSL §3.3.1 Introduction to Ridge、§3.3.2 The Shrinkage Effect、§3.3.4 Degree-of-Freedom**
（<https://liangfgithub.github.io/PSL/w3/w3_3_ridge.html>）

> "In ridge regression, the objective function to be minimized is a smooth quadratic function of beta. This function consists of two terms: the residual sum of squares and the L2 norm of beta."
>
> "A key benefit of ridge regression is its applicability to design matrices that are not of full rank. This is made possible by adding $\lambda \mathbf{I}$ to $\mathbf{X}^t \mathbf{X}$ , which allows us to invert the resulting matrix. This feature sets ridge regression apart from ordinary least squares (OLS) methods, making it a versatile tool for tackling a wide range of problems."
>
> "In ridge regression, the coefficients and predictions are shrinkage versions of those in OLS. The degree of shrinkage varies with the magnitude of singular values, shrinking less for larger singular values and more for smaller ones."
>
> "A commonly-adopted metric for the degree of freedom involves the sum of the normalized variance between the observed $y_i$ and its corresponding predicted value $\hat{y}_i$ , across all n samples. In matrix notation, if a method returns $\hat{\mathbf{y}} = \mathbf{A} \mathbf{y}$ , the degree of freedom can be computed as the trace of matrix A."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $\hat{\boldsymbol{\beta}}^{\text{ridge}} = (\mathbf{X}^t \mathbf{X} + \lambda \mathbf{I})^{-1} \mathbf{X}^t \mathbf{y}$；等价于对增广数据 $\left(\begin{array}{c} \mathbf{y} \\ \mathbf{0}_{p} \end{array}\right) = \left(\begin{array}{c} \mathbf{X} \\ \sqrt{\lambda} \mathbf{I}_p \end{array}\right) \boldsymbol{\beta} + \text{error}$ 做 OLS。
- 正交设计下 $\hat{\beta}^{\text{ridge}}_j = \frac{1}{1+\lambda}\hat{\beta}^{\text{LS}}_j$；有效自由度 $\text{df}(\lambda) = \text{tr}(\mathbf{X}(\mathbf{X}^t\mathbf{X}+\lambda\mathbf{I})^{-1}\mathbf{X}^t) = \sum_j \frac{d_j^2}{d_j^2+\lambda}$，取值连续落在 $[0, p]$。
- （通用形式，非某本教材独有；$\hat\beta^{ridge}$ 与增广写法见 PSL §3.3.1 原文公式。）

**中文解释**

Ridge 在 RSS 上加 $L_2$ 惩罚，把系数按奇异值大小「按比例缩小」（大奇异值缩得少）。好处是 $\mathbf{X}^t\mathbf{X}+\lambda\mathbf{I}$ 一定可逆，能处理共线性与 $p>n$。代价是引入偏差，但有效自由度变成 $0$ 到 $p$ 之间的实数。

**面试怎么用**

「为什么要收缩？最小二乘不是无偏吗」——答：MSE = Bias$^2$ + Variance，收缩用一点偏差换更多方差下降；再补一句它是 "a smooth quadratic function of beta"，有闭式解。

---

### B9. Lasso 与稀疏性

**教材原文 · PSL §3.4.1 Introduction to Lasso、§3.4.2 One-dimensional Lasso、§3.4.3 Lasso vs. Ridge**
（<https://liangfgithub.github.io/PSL/w3/w3_4_lasso.html>）

> "The Lasso objective function consists of two terms: The residual sum of squares (RSS) The L1 norm of the coefficient vector beta"
>
> "So, in an orthogonal design matrix, the Lasso solution can be computed using the soft-thresholding operator. It performs both variable selection and coefficient shrinkage. This is one of the reasons why Lasso is a preferred method because it can both select important features and shrink the coefficients towards zero, making it particularly useful in practice."
>
> "Lasso : The optimization task involves minimizing the residual sum of squares (RSS) coupled with a penalty term on the L1 norm of the coefficients. This is mathematically equivalent to a constrained minimization of RSS, where the coefficients (beta) are restricted to lie within an L1 ball (a diamond-shaped constraint)."
>
> "Lasso : Implements “soft-thresholding.” Increasing lambda not only shrinks the coefficients but can also set some to zero, effectively performing variable selection."
>
> "Subset Selection : Operates as a “hard-thresholding” mechanism, where coefficients either remain at their original value or are set to zero."
>
> "For general design matrices, Lasso does not have a closed-form solution; that is, one cannot employ analytical methods to directly find the optimal coefficients. A popular technique for solving the Lasso problem is the Coordinate Descent algorithm."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $\min_{\boldsymbol{\beta}} \| \mathbf{y} - \mathbf{X} \boldsymbol{\beta}\|^2 + \lambda \| \boldsymbol{\beta} \|_1$
- 正交设计下的解（soft-thresholding）：$\hat{\beta}^{\text{lasso}}_j = \text{sign}(\hat{\beta}_j^{\text{LS}}) (|\hat{\beta}_j^{\text{LS}}| - \lambda/2)$ if $|\hat{\beta}_j^{\text{LS}}| > \lambda/2$，否则 $0$。
- 等价约束形式 $\min \| \mathbf{y} - \mathbf{X} \boldsymbol{\beta}\|^2$ s.t. $\| \boldsymbol{\beta} \|_1 \le t$（$L_1$ 球/菱形）。
- （通用形式，非某本教材独有；软阈值公式即 PSL §3.4.2 原文。）

**中文解释**

Lasso 用 $L_1$ 惩罚，等价于把系数限制在菱形 $L_1$ 球里；菱形的顶点在坐标轴上，所以解容易「卡在」某个系数为 0，实现稀疏与变量选择。它没有一般闭式解，要用坐标下降。与 ridge 的线性收缩不同，软阈值是「小于阈值直接归零」。

**面试怎么用**

「为什么 lasso 能做变量选择而 ridge 不行」——几何答案就是 L1 ball (a diamond-shaped constraint) vs L2 ball；再补 soft / hard-thresholding 的对照。

---

### B10. Ridge vs Lasso 的取舍

**教材原文 · PSL §3.5 Discussion（页面 w3_5_discussion.html）**
（<https://liangfgithub.github.io/PSL/w3/w3_5_discussion.html>）

> "Thus far, we have explored a variety of techniques for variable selection and shrinkage, including subset selection, ridge regression, lasso regression, and principal components regression ( Notes on PCR ). A critical question emerges: which method is most appropriate for a given situation?"
>
> "For X2, where features are correlated yet mostly relevant to Y, shrinkage methods like ridge regression and methods based on top PCs such as PCR can be effective."
>
> "For X3, ridge regression and PCR are less effective as they tend to prioritize early principal components. Given that much of the variation in X3 is noise-driven, the top principal components are not necessarily relevant to Y. Here, methods like Lasso, which can completely zero out specific features, become increasingly important."
>
> "For X1, the feature set is small and carefully curated, often obviating the need for variable shrinkage or selection; a full model may be sufficient."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 同一 $\lambda$ 下的比较（通用形式）：$\text{Ridge}$ 用 $\| \boldsymbol{\beta} \|^2$，$\text{Lasso}$ 用 $\| \boldsymbol{\beta} \|_1$，Elastic Net 用 $\lambda_1 \| \boldsymbol{\beta} \|_1 + \lambda_2 \| \boldsymbol{\beta} \|^2$。
- （通用形式，非某本教材独有；Elastic Net 不在 PSL §3.5 正文中。）

**中文解释**

PSL 的取舍逻辑按设计矩阵分三种情形：特征少且挑过 → 全模型即可；特征相关但都有关（$X_2$）→ ridge / PCR 这类收缩方法有效；混入大量噪声特征（$X_3$）→ ridge/PCR 因为优先保留大方差的早期主成分反而更差，必须用能真正置零的 lasso。

**面试怎么用**

被问「ridge 还是 lasso」时，别只背「稀疏 vs 不稀疏」，按「特征是否相关、是否有大量无关噪声」分情形回答，这正是 PSL 给出答案的方式。

---

## C. 树与集成

### C11. 回归树 / 递归二分

**教材原文 · PSL §4.1.1–§4.1.2（页面 w4_1_reg_tree.html）**
（<https://liangfgithub.github.io/PSL/w4/w4_1_reg_tree.html>）

> "Trees models are constructed by recursively splitting regions of X into two sub-regions, beginning with the entire feature space X. For simplicity, we’ll focus on recursive binary partitions."
>
> "When you focus on minimizing the squared error between observed and predicted values, the best prediction for each leaf node is simply the average response of the samples in that node."
>
> "Trees are built in a top-down and greedy fashion. For each node, we try all possible splits (both features and split values) and pick the one that provides the most significant gain in RSS."
>
> "Each leaf node corresponds to a rectangular-shaped region in the feature space."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 回归树 $\hat{f}(x) = \sum_{m=1}^{M} c_m \mathbf{1}\{x \in R_m\}$，$c_m = \text{ave}(y_i \mid x_i \in R_m)$。
- 分裂准则（最小化子节点平方误差和）$\min_{j,\,s} \Big[ \min_{c_L} \sum_{x_i \in R_L(j,s)} (y_i - c_L)^2 + \min_{c_R} \sum_{x_i \in R_R(j,s)} (y_i - c_R)^2 \Big]$。
- （通用形式，非某本教材独有；PSL 正文用「gain in residual sum of squares」表述同一准则。）

**中文解释**

回归树把特征空间递归二分，每个叶节点是一个矩形区域，区域内用响应均值作为常数预测。分裂变量与切点靠贪心地最大化 RSS 的下降量来选。优点是可解释、自动做变量选择、不要求变量变换（对单调变换不变）。

**面试怎么用**

被问「树怎么长」时按三问回答：Where to Split（gain in RSS）、When to Stop（阈值或先长后剪）、How to Predict（leaf 均值）。

---

### C12. 代价复杂度剪枝（CCP）/ weakest link / 1-SE 原则

**教材原文 · PSL §4.1.3 Complexity Cost、§4.1.4 Weakest Link Algorithm、§4.1.5 Cross-Validation；1-SE 定义引 ESL §7.10（p. 244）**
（<https://liangfgithub.github.io/PSL/w4/w4_1_reg_tree.html>）

> "The complexity cost for a tree T is defined as: $R_{\alpha}(T) = \text{RSS}(T) + \alpha |T|$"
>
> "… is the number of leaf nodes in the tree. This quantifies the size or complexity of the tree."
>
> "The Weakest Link Algorithm can be used to find the optimal subtree for all possible values of $\alpha$ . The algorithm proceeds as follows:"
>
> "The algorithm generates a sequence of increasingly smaller trees corresponding to increasing $\alpha$ values, starting from the largest tree down to a single-node tree (the root)."
>
> "Finally, we select the optimal $\alpha$ value based on either the minimum CV error or using the one standard error principle."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 复杂度代价 $R_{\alpha}(T) = \text{RSS}(T) + \alpha \vert T\vert$；$\vert T\vert$ 是叶节点数，$\alpha \ge 0$ 控制拟合与复杂度的权衡。
- 对每个内部节点 $t$，weakest link 计算 $\alpha^* = \frac{\text{RSS}(t) - \text{RSS}(T_t)}{\vert T_t \vert - 1}$（剪掉该枝「最便宜」的代价），取最小的 $\alpha_1$ 依次剪枝，得到随 $\alpha$ 递增的子树序列。
- 1-SE 原则（**ESL §7.10, p.244 逐字原文**）：
  > "Often a “one-standard error” rule is used with cross-validation, in which we choose the most parsimonious model whose error is no more than one standard error above the error of the best model."
  >
  > 该规则的**理由**（ESL §3.4.3, p.61）："Such a rule acknowledges the fact that the tradeoff curve is estimated with error, and hence takes a conservative approach."
  >
  > 出处自证：ESL 在 §3.4.3 明确写出「(Section 7.10, page 244)」指向该定义。原文 PDF：<https://hastie.su.domains/ElemStatLearn/>
  > —— 中文：在 CV 曲线上先找最小误差，再选误差不超过「最小误差 + 1 个标准误」的**最简**模型。
- （$R_\alpha(T)$ 与 weakest link 是 PSL/ESL 的记法；$\alpha^*$ 的闭式表达属通用形式。）

**中文解释**

CCP 用 $\text{RSS}(T)+\alpha|T|$ 给树定价：$\alpha$ 大则树小。Weakest Link 每步剪掉「每单位复杂度换来的 RSS 改善最少」的那一枝，从而生成一整条子树路径（含单节点树）。$\alpha$ 由 K 折 CV 选，可以用最小值，也可以用 1-SE 原则选更稳的小树。

**面试怎么用**

考「剪枝」时三步走：代价复杂度公式 → weakest link 生成 $\alpha$ 路径 → CV（或 1-SE）选 $\alpha$，并指出最简最优子树的存在性保证了唯一性。

---

### C13. 随机森林 / m 的推荐值 / OOB / 两种重要性

**教材原文 · PSL §4.2.1 Introduction to Random Forest、§4.2.3 Variable Importance（页面 w4_2_randomforest.html）**
（<https://liangfgithub.github.io/PSL/w4/w4_2_randomforest.html>）

> "Bagging (Bootstrap Aggregation) is a technique where we generate multiple bootstrap samples, fit a regression tree model to each, and average the predictions from these trees. Specifically, for each of the ‘B’ bootstrap samples, we fit the data to a regression tree to obtain individual trees T1, T2, …, TB. The final prediction is the average of the predictions from these ‘B’ trees."
>
> "These omitted data points are referred to as “ Out-of-Bag ” (OOB) samples."
>
> "Random Forest improves upon simple bagging by introducing constraints aimed at decorrelating the individual trees."
>
> "The key constraint in Random Forest is that at each split, only a random subset of ‘m’ features is considered out of the total ‘p’ features. The optimal split is then chosen among these ‘m’ features. The recommended values for ‘m’ are the square root of ‘p’ for classification tasks and ‘p/3’ for regression tasks."
>
> "One of the primary reasons for this constraint is to minimize the correlation between individual trees in the forest. If all trees were trained using all features, they would likely make similar splits and, consequently, make highly correlated errors. By subsetting the features at each split, the trees are “forced” to consider different features for making a split. This results in different trees focusing on different aspects of the data, which makes the ensemble more diverse and robust."
>
> "The first measure evaluates a variable’s importance based on the reduction in the residual sum of squares (RSS) it contributes to each tree. Whenever a split is made in a tree using a particular variable, RSS decreases. Those reductions are are aggregated across all the trees in the forest for each variable. The average RSS improvement gives us an indication of a variable’s importance."
>
> "The second measure focuses on prediction accuracy, using OOB samples. The process involves random shuffling of the j-th predictor and recording the new prediction error. The difference between the two prediction errors, before and after permutation, gives the importance measure for the j-th predictor variable."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 袋外样本概率：每个 $x_i$ 不被某次 bootstrap 抽中的概率 $\approx (1 - 1/n)^n \to e^{-1} \approx 0.368$。
- 袋外误差 $\text{OOB-Err} = \frac{1}{n}\sum_{i=1}^n L\big(y_i, \hat{f}^{[-i]}(x_i)\big)$，$\hat{f}^{[-i]}$ 只用不含第 $i$ 个观测的树。
- 重要性 I：分裂带来的 RSS 下降（按变量归集后取平均）；重要性 II：置换前后 OOB 预测误差之差，$\text{VI}_j = \frac{1}{B}\sum_b (\text{Err}^{\text{perm}}_b - \text{Err}_b)$。
- （通用形式，非某本教材独有；两条重要性名称沿用 PSL 的 RSS Gain 与 Accuracy Gain Before/After Permutation。）

**中文解释**

Bagging 抽 B 个 bootstrap 样本各长一棵树再平均，专治高方差低偏差的树；随机森林再多加一层限制：每次分裂只看随机抽出的 $m$ 个特征（分类 $\sqrt{p}$、回归 $p/3$），把树之间的相关性压下来。没被抽中的 OOB 样本天然可当验证集。重要性有两套：分裂的 RSS 收益，和 OOB 上打乱该变量后误差的上升。

**面试怎么用**

被追问 m 的取值时，给出原文推荐值（$\sqrt{p}$ / $p/3$）并解释动机是 "decorrelating the individual trees"；再提一句高基数分类变量的重要性会被夸大。

---

### C14. GBM / 学习率 eta / 调参要点

**教材原文 · PSL §4.3.1 Boosting、§4.3.3 Discussion（页面 w4_3_gbm.html）**
（<https://liangfgithub.github.io/PSL/w4/w4_3_gbm.html>）

> "At its core, boosting is a forward stage-wise greedy algorithm aimed at solving an additive model. An additive model consists of ‘T’ functions, $F(x) = f_1(x) + f_2(x) + \cdots + f_T(x)$ and you can think of each function as corresponding to a regression tree."
>
> "However, a word of caution: boosting can easily lead to overfitting. This is why regularization is critical. In Gradient Boosting Machines (GBM) or Extreme Gradient Boosting (XGBoost), packages for boosting, one of the key regularization parameters is the learning rate, ‘eta’, which shrinks the contribution of each individual tree."
>
> "From an optimization standpoint, you could also view ‘eta’ as the “learning rate” that governs how big a “step” the algorithm should take in the optimal direction."
>
> "It’s worth noting that the choice of T is often inversely related to the learning rate eta. Specially, a smaller learning rate generally necessitates a larger T for the model to effectively learn and generalize."
>
> "When it comes to the complexity of the individual trees, i.e., their depth, it’s advisable to keep them relatively shallow, as boosting is designed to work with weak learners."
>
> "Now, comparing GBM and RandomForest: RandomForest boasts fewer tuning parameters. GBM has more. However, with the right tuning, GBM can outperform RandomForest."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 加性模型 $F(x) = f_1(x) + f_2(x) + \cdots + f_T(x)$；第 $t$ 步把树加到当前模型上，$F_t(x) = F_{t-1}(x) + \eta\, f_t(x)$，$f_t$ 拟合当前残差（梯度提升下为伪残差）。
- （$F_t = F_{t-1} + \eta f_t$ 是通用形式；PSL §4.3.1 原文写作「Add the tree to the target function F(x)」并把 $\eta$ 描述为收缩每棵树贡献的学习率。）

**中文解释**

GBM 是前向分步的贪心算法：每一步用一棵小树去拟合当前残差并加到模型里。$\eta$ 收缩每棵树的贡献，既是学习率也是防过拟合的正则化；$\eta$ 小则需要的树数 $T$ 大（反向关系），树要浅（弱学习器），还可以加子采样。RF 调参更少更稳，GBM 调好了通常更强——PSL 的原话。

**面试怎么用**

「GBM 怎么调参」的标准骨架：学习率 $\eta$ ↓ → 树数 $T$ ↑、深度保持浅、加子采样；用 CV 误差早停。

---

### C15. 分类树的不纯度（entropy / Gini / misclassification）

**教材原文 · PSL §12.2 Impurity Measures（页面 w12_2_measures.html）**
（<https://liangfgithub.github.io/PSL/w12/w12_2_measures.html>）

> "The impurity measure is a function $I(p_1, \dots, p_K)$ defined over a probability distribution representing K classes."
>
> "It reaches its maximum value when all classes are equally likely and its minimum when only one class is certain (i.e., $p_j$ equals one for one class)."
>
> "Once we have defined the impurity measure, we can derive the goodness-of-split criterion"
>
> "The criteria $\Phi,$ representing the gain of the impurity measure, is computed as the difference between i) the impurity measure when no split is applied and ii) the weighted sum of impurity measures in the left and right nodes after a split."
>
> "In this measure, majority voting is used, and the class corresponding to the maximum $p_j$ is considered correct."
>
> "It’s important to note that entropy is a strictly concave function, which means it strongly favors splits leading to pure nodes."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 不纯度 $I(p_1, \dots, p_K)$：$\text{Misclassification Rate}: 1 - \max_j p_j$；$\text{Entropy (Deviance)}: - \sum_{j=1}^K p_j \log p_j$；$\text{Gini index}: \sum_{j=1}^K p_j(1-p_j) = 1 - \sum_j p_j^2$。
- 分裂增益 $\Phi(j,s) = i(t) - \big[ p_R \cdot i(t_R) + p_L \cdot i(t_L) \big]$，$i(t) = I(p_t(1), \dots, p_t(K))$，$p_t(j)$ 为节点 $t$ 上类别 $j$ 的频率。
- （通用形式，非某本教材独有；三行公式即 PSL §12.2.3 原文。）

**中文解释**

不纯度是定义在 $K$ 类概率分布上的对称函数：均匀分布时最大（最不纯），某一类概率为 1 时最小（最纯）。分裂增益是父节点不纯度减去左右子节点不纯度的加权和。三个常用度量是误分类率、熵（deviance）与 Gini，熵和 Gini 严格凹，更鼓励长出纯节点。

**面试怎么用**

考「分类树分裂准则」时先给不纯度三要件（maximum at uniform、minimum at pure、symmetric），再写 $\Phi(j,s)$ 与三个公式。

---

### C16. 误分类率 vs 熵（严格凹 vs 分段线性）

**教材原文 · PSL §12.3 Misclassification Rate vs. Entropy（页面 w12_3_compare.html）**
（<https://liangfgithub.github.io/PSL/w12/w12_3_compare.html>）

> "Crucially, the gain in the impurity measure will be positive if the function f is strictly concave. This property ensures that the difference between impurity measures at the parent node and the child nodes is always positive when evaluating the gain."
>
> "The misclassification rate (blue curve) is piecewise linear, while entropy (red curve) is strictly concave."
>
> "However, if both p1 and p2 fall on the same side of 0.5 (for instance, both on the left side), using the misclassification rate results in a zero gain because there is no additional benefit gained from the split. Conversely, entropy (and Gini index) consistently yield positive gains, encouraging the creation of pure nodes."
>
> "In summary, when constructing the tree, it is advisable to use either entropy or the Gini index as impurity measures because they are strictly concave functions. They encourage the creation of pure nodes."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 以两类、负面类比例 $p$ 记：误分类率 $f(p) = \min(p, 1-p)$（分段线性，$p = 0.5$ 处不可导）；熵 $f(p) = -p\log p - (1-p)\log(1-p)$（严格凹）。
- 严格凹保证 $f\big(\frac{n_R p_{t_R} + n_L p_{t_L}}{n_R+n_L}\big) \ge \frac{n_R f(p_{t_R}) + n_L f(p_{t_L})}{n_R+n_L}$，故增益 $\Phi > 0$。
- （通用形式，非某本教材独有。）

**中文解释**

误分类率是分段线性的，当左右子节点的多数类相同时增益为 0，树「看不出」这次分裂的价值；熵（和 Gini）严格凹，由 Jensen 不等式保证任何分裂的增益都严格为正，因此倾向把节点越切越纯。所以建树用熵/Gini，剪枝阶段可以换回误分类率。

**面试怎么用**

这是高频追问：用 "piecewise linear vs strictly concave" + "zero gain on the same side of 0.5" 两个原话回答，比抽象讲信息增益更有说服力。

---

### C17. AdaBoost 算法与训练误差上界

**教材原文 · PSL §12.4.1 The Algorithm、§12.4.2 Proof（页面 w12_4_aAaboost.html）**
（<https://liangfgithub.github.io/PSL/w12/w12_4_aAaboost.html>）

> "It’s worth noting that AdaBoost is essentially a gradient-based algorithm, aiming to fit the model using an exponential loss function."
>
> "The goal of AdaBoost is to combine weak classifiers, which are classifiers that don’t need to be exceptionally accurate, as long as they perform slightly better than random guessing."
>
> "The core idea of the algorithm is to iteratively modify the weights assigned to the training data."
>
> "Next we demonstrate that the training error can be driven to zero as the number of iterations ‘T’ approaches infinity, even when the individual classifiers are weak."
>
> "It’s important to note that that the training error of the combined classifier G_T generated by AdaBoost is not guaranteed to be monotonically decreasing with respect to the number of iterations. Instead, after each iteration, AdaBoost decreases a specific upper bound on the 0/1 training error."
>
> "AdaBoost can utilize weak classifiers, denoted by $g_t(x)$ , even if their error rate is worse than random guessing (i.e., $\epsilon_t > 1/2$ ). In such cases, AdaBoost adapts by assigning a negative weight $\alpha_t$ , effectively using"

**标准记法**（通用形式，非某本教材独有，除非注明）

- 最终分类器 $G_T(x) = \text{sign}\big( \sum_{t=1}^T \alpha_t g_t(x_i) \big)$，$y_i \in \{+1, -1\}$；权重 $\alpha_t = \frac{1}{2} \log \frac{1-\epsilon_t}{\epsilon_t}$。
- 训练误差上界 $\text{Training-Err}(G_T) = \sum_i \frac{1}{n} I\big(y_i \ne \text{sign}(\sum_{t=1}^T \alpha_t g_t(x_i))\big) \le \prod_{t=1}^T Z_t$，$Z_t = 2\sqrt{\epsilon_t(1-\epsilon_t)} < 1$。
- 上界不要求单项误差下降：每一步降低的是 0/1 误差的上界，$T \to \infty$ 时训练误差趋于 0；若某轮 $\epsilon_t > 1/2$，AdaBoost 给出负的 $\alpha_t$，等价于用 $-g_t(x)$。
- （公式即 PSL §12.4.2 原文；$\alpha_t$ 的闭式见 PSL §12.5 原文。）

**中文解释**

AdaBoost 反复调样本权重、串起一堆「比乱猜略好」的弱分类器，等价于用指数损失做梯度式拟合。可以证明训练误差上界是 $\prod_t Z_t$，每一步都严格小于 1，所以 $T$ 增大训练误差趋 0。注意：下降的是**上界**，训练误差本身不必单调；而且弱分类器差于随机猜（$\epsilon_t>1/2$）也能用，因为 $\alpha_t$ 会取负。

**面试怎么用**

面试要能背出 $Z_t = 2\sqrt{\epsilon_t(1-\epsilon_t)} < 1$ 这个「每轮都小于 1」的结论，并主动澄清「单调下降的是上界」。

---

### C18. 前向分步加性模型（forward stagewise additive modeling）

**教材原文 · PSL §12.5 Forward Stagewise Additive Modeling（页面 w12_5_boost.html）**
（<https://liangfgithub.github.io/PSL/w12/w12_5_boost.html>）

> "In the context of boosting, we’re essentially looking to combine multiple functions into a stronger model. Consider an additive model"
>
> "It is challenging to optimize this function, since we have to consider not only the alpha values but also optimize the functions g themselves. The approach often used here is Forward Stagewise Optimization , which begins with a baseline of no functions, then incrementally adds to the model by optimizing one weight and one function at a time, keeping previously selected elements fixed."
>
> "Boosting algorithms can take various forms, depending on the choice of the base model $g_t$ , the choice of the loss function $L(y, f(x)),$ and how optimization is done."
>
> "When doing the optimization with exponential loss, the effect of the previous (t-1) functions becomes updating the weights for each data point. Let $\epsilon_t$ denote the corresponding weighted empirical error rate"
>
> "With regression , the process is similar but typically involves a square loss function. At the t-th iteration, $(y_i - f_{t-1}(x_i) - \alpha g(x))^2 = (r_{it} - \alpha g(x))^2.$ The effect of the previous (t-1) functions becomes partial residuals, and the goal is to fit the new function to the current partial residuals $r_{it}.$"
>
> "GBM directly uses the gradient as the pseudo-residual to fit a regression tree, while XGBoost involves a more complex formulation."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 加性模型 $f(x) = \alpha_1 g_1(x) + \alpha_2 g_2(x) + \cdots + \alpha_T g_T(x)$；每步只优化一个 $\alpha$ 和一个 $g$，保持已有项固定。
- AdaBoost：指数损失下的最优 $\alpha_t = \frac{1}{2} \log \frac{1-\epsilon_t}{\epsilon_t}$。
- 回归/平方损失：$(y_i - f_{t-1}(x_i) - \alpha g(x))^2 = (r_{it} - \alpha g(x))^2$，即对当前「部分残差」$r_{it}$ 拟合新函数。
- （公式即 PSL §12.5 原文；「伪残差」梯度提升的通用写法为 $r_{it} = -\big[\partial L(y_i, f(x_i))/\partial f(x_i)\big]_{f = f_{t-1}}$。）

**中文解释**

前向分步加性模型把一个难解的整体优化拆成一串单步优化：每次只挑一个基函数 $g_t$ 与系数 $\alpha_t$，前面的项不动。不同的损失（指数 → AdaBoost；平方 → 回归提升）、不同的 $g_t$、不同的优化方式，就得到不同的提升算法；没有简单闭式时用泰勒展开近似，这就是梯度提升。

**面试怎么用**

被问「AdaBoost 和 GBM 什么关系」时，答：两者都是 forward stagewise additive modeling 的特例，区别只在损失函数（exponential vs 一般可微损失）与是否用梯度近似。

---

## D. 非线性与判别

### D19. 多项式回归

**教材原文 · PSL §5.1.1 Fit a Polynomial Regression Model、§5.1.2 Choosing the Degree**
（<https://liangfgithub.github.io/PSL/w5/w5_1_poly.html>）

> "Polynomial regression is a type of regression analysis used when the relationship between the independent variable x and the dependent variable y is non-linear."
>
> "Polynomial regression can be seen as a specialized case of multiple linear regression."
>
> "This is because the coefficients depend on the choice of the basis function. Different methods can yield different coefficients but result in the same predicted curve."
>
> "Note that the residual sum of squares, being a measure of training error, is unsuitable for this purpose. Employing it could misleadingly favor the highest degree."
>
> "Global Assumptions : Polynomial regression inherently posits a global assumption about the mean function, which represents the expected value of the response variable Y given X. Such global functions can lack flexibility, particularly when data exhibits localized fluctuations."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $y_i = \beta_0 + \beta_1 x_i + \beta_2 x_i^2 \cdots + \beta_d x_i^d + \text{err}$，设计矩阵列向量为 $(1, x_i, x_i^2, \dots, x_i^d)$。
- （通用形式，非某本教材独有；与 PSL §5.1.1 原文公式一致。）

**中文解释**

多项式回归是多元线性回归的特例：把 $x, x^2, \dots, x^d$ 当作新特征，系数仍用最小二乘估。选阶数 $d$ 不能用 RSS（会偏向高阶），要用前向/后向检验、AIC/BIC 或交叉验证。缺点是全局假设、外推时尾部由高阶项主导、且系数依赖基函数选择而不易解释。

**面试怎么用**

「多项式回归还是线性模型吗」——是，"a specialized case of multiple linear regression"；再补三点局限（interpretability / tail behavior / global assumptions）。

---

### D20. 样条（回归样条 / 自然三次样条）

**教材原文 · PSL §5.2.1 Cubic Splines、§5.2.2 Linear Subspace、§5.2.3 Natural Cubic Splines**
（<https://liangfgithub.github.io/PSL/w5/w5_2_spline.html>）

> "A cubic spline is a function that’s defined piecewise by individual cubic polynomials. Specifically, for any interval divided by m knots, the cubic spline ensures smoothness by being not only continuous but also having continuous first and second derivatives at every knot."
>
> "A natural cubic spline is a special kind of cubic spline. While it retains the property of being a piecewise cubic polynomial function with continuity up to the second derivative, it exhibits linear behavior in the two extreme intervals."
>
> "So, the effective degree of freedom, or the number of independent parameters, for a cubic spline with m knots is 4(m+1)-3m = m+4."
>
> "Consequently, the degree of freedom for a natural cubic spline is m."
>
> "The dimension of this linear subspace is m+4, established from our earlier discussion. This means we can select m+4 linearly independent functions from this space, which serve as a basis."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 三次样条基（PSL §5.2.2 原文给出的一组）：$h_0(x) = 1$，$h_1(x) = x$，$h_2(x) = x^2$，$h_3(x) = x^3$，$h_{i+3}(x) = (x-\xi_i)_{+}^3, \ i = 1, 2, \dots, m$。
- 自由度：三次样条 $4(m+1) - 3m = m+4$；自然三次样条（两端区间线性）为 $m$。
- （通用形式，非某本教材独有；基函数与自由度结论即 PSL §5.2 原文。）

**中文解释**

样条是分段多项式，在结点处拼接并保证到二阶导连续（因此看起来光滑、不像分段线性那样有折角）。自然三次样条额外要求两端区间是线性的，把自由度从 $m+4$ 降到 $m$，外推时也更稳。给定结点后所有三次样条构成线性子空间，所以可以直接当线性模型用最小二乘拟合。

**面试怎么用**

「样条为什么比多项式好」——分段局部拟合避免了全局假设；「为什么用自然样条」——省 4 个自由度且边界线性、外推不爆炸。

---

### D21. 平滑样条与 GCV

**教材原文 · PSL §5.4.2 The Roughness Penalty Approach、§5.4.3 Fit a Smoothing Spline Model、§5.4.5 Choice of Lambda**
（<https://liangfgithub.github.io/PSL/w5/w5_4_smoothing_spline.html>）

> "As a solution to the arbitrariness of knot placement in regression splines, smoothing splines take a naive approach. Knots are placed at every data point, simplifying the decision-making process."
>
> "Instead of merely trying to fit the data as closely as possible, let’s look for the minimizer in $S[a,b]$ of the following penalized residual sum of squares"
>
> "The second term is a roughness penalty on function $g$ . The second derivative is a measure of the curvature or “wiggliness” of a function."
>
> "The best smooth function $g$ that minimizes the penalized residual sum of squares over the entire infinite-dimensional function space $S[a,b]$ happens to be a natural cubic spline (NCS) with knots at the data points x_1, …, x_n"
>
> "The effective degree of freedom (DF) of a smoothing spline is similar to what’s defined for ridge regression. It’s the trace of the smoother matrix"
>
> "In some cases, GCV is used to lessen computational demands. Instead of using varying $S_{ii}$ values (which are different for each i), we take their average, which serves as an approximation."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $\text{RSS}_\lambda(g) = \sum_{i=1}^n [y_i - g(x_i)]^2 + \lambda \int_a^b [g''(x)]^2 d x$，$g \in S[a,b]$；$\lambda$ 控制光滑程度。
- 解为以数据点为结点的自然三次样条：$g(x) = \sum_{i=1}^n \beta_i h_i(x)$；矩阵形式 $\min_{\boldsymbol{\beta}} \big[ \| \mathbf{y} - \mathbf{F} \boldsymbol{\beta} \|^2 + \lambda \boldsymbol{\beta}^t \Omega \boldsymbol{\beta} \big]$，与 ridge 同构。
- 有效自由度 $\text{df}_\lambda = \text{tr}(\mathbf{S}_\lambda)$，取值在 $2$ 与 $n$ 之间（线性项不被惩罚）。
- GCV（通用形式）：$\text{GCV}(\lambda) = \frac{1}{n} \sum_{i=1}^n \big(y_i - \hat{g}(x_i)\big)^2 \big/ \big(1 - \text{tr}(\mathbf{S}_\lambda)/n\big)^2$，用 $\bar{S}_{ii}$ 的平均替掉逐点的 $S_{ii}$。
- （前两行与痕式 DF 见 PSL §5.4 原文；GCV 的具体表达式属通用形式，PSL 只说明它是「对 $S_{ii}$ 取平均」的近似。）

**中文解释**

平滑样条把结点放在每个数据点上，用粗糙度惩罚 $\lambda\int [g'']^2$ 来控制弯曲程度，等价于「把自然三次样条当 ridge 来拟合」。$\lambda$ 决定有效自由度（$2$ 到 $n$ 之间），常用 LOO-CV 或 GCV 选（GCV 是 LOO 的廉价近似）。

**面试怎么用**

把 smoothing spline 一句话定位成「ridge 的函数版」，并给出 $\text{RSS}+\lambda\int[g'']^2$ 与 $\text{df}=\text{tr}(S_\lambda)$ 这两个可背诵的公式。

---

### D22. 判别分析 / LDA / QDA

**教材原文 · PSL §9.2 Discriminant Analysis、§9.3 QDA、§9.4 LDA（页面 w9_2_DA.html / w9_3_QDA.html / w9_4_LDA.html）**
（<https://liangfgithub.github.io/PSL/w9/w9_2_DA.html>）

> "We aim to learn the joint distribution $p(x,y)$ by breaking it down into two parts:"
>
> "This approach, which involves learning the classifications in this manner, is commonly referred to as discriminant analysis"
>
> "Our primary objective is to calculate the posterior probability $P(Y=k | X=x)$"
>
> "This classification rule, described here, is known as quadratic discriminant analysis (QDA) . The key characteristic of QDA is that the function used to make decisions is quadratic in terms of the feature vector"
>
> "When we assume that all covariance matrices are the same for all classes, we essentially revert to Linear Discriminant Analysis (LDA) since the discriminant function becomes linear."

**标准记法**（通用形式，非某本教材独有，除非注明）

- Bayes 决策函数 $\arg\max_k \pi_k f_k(x) = \arg\max_k \big[ \log \pi_k + \log f_k(x) \big]$，$\pi_k = P(Y=k)$，$f_k(x) = p(x \mid Y=k)$。
- QDA：$f_k(x)$ 为 $N(\mu_k, \Sigma_k)$，判别分 $d_k(x) = (x-\mu_k)^t \Sigma_k^{-1} (x-\mu_k) + \log \vert \Sigma_k \vert - 2 \log \pi_k$。
- LDA：$\Sigma_k = \Sigma$，判别分对 $x$ 线性（第一项中 $x^t \Sigma^{-1} x$ 与 $k$ 无关可消去）；$\hat{\Sigma} = \frac{1}{n-K} \sum_{k=1}^K \sum_{i: y_i=k} (x_i - \hat{\mu}_k)(x_i - \hat{\mu}_k)^t$。
- （通用形式，非某本教材独有；判别分与 pooled 协方差即 PSL §9.3/§9.4 原文公式。）

**中文解释**

判别分析是「生成式」路线：估先验 $\pi_k$ 与类条件密度 $f_k(x)$，再用 Bayes 定理得到后验做决策，而不是直接建模 $P(Y=k|X=x)$。假设 $f_k$ 是多元正态：协方差各自不同 → 判别函数是二次的（QDA）；假设各类共享同一个 $\Sigma$ → 判别函数线性（LDA），可用 pooled 样本协方差估计。

**面试怎么用**

被问 LDA/QDA 区别时，唯一的判据就是「协方差矩阵是否共享」：shared $\Sigma$ → linear，否则 quadratic；再补两者都能给出后验概率而非只给硬分类。

---

### D23. 朴素贝叶斯分类器

**教材原文 · PSL §9.6 Naive Bayes Classifiers（页面 w9_6_NB.html）**
（<https://liangfgithub.github.io/PSL/w9/w9_6_NB.html>）

> "The “Naive” part of Naive Bayes comes from the assumption of feature independence. It assumes that the features are conditionally independent of each other given the class label. This simplifies the computation and makes it computationally efficient."
>
> "In Naive Bayes, we factorize the joint distribution function into individual one-dimensional distribution functions for these p elements. This concept can be seen as a way to introduce independence as a form of regularization for high-dimensional problems."
>
> "To test your understanding: When employing parametric Naive Bayes, how many parameters must we estimate? We indeed need p means, one for each dimension, and p variances, one for each dimension, totaling 2p parameters."
>
> "Some Naive Bayes packages may compute this term using the first line, i.e., by evaluating the normal probability density function (pdf) and then taking the logarithm. When $x^*$ is far from the center, the normal pdf can become very small, leading to numerical issues when applying the logarithm."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 决策函数 $\arg\max_k P(Y=k | X=x) = \arg\max_k \pi_k f_k(x)$；朴素贝叶斯假设 $f_k(x) = f_{k1}(x_1) \times f_{k2}(x_2) \cdots \times f_{kp}(x_p)$。
- 参数化版本（高斯）判别分 $d_k(x^*) = - \log \pi_k + \frac{1}{2} \sum_{j=1}^p \big[ \log \sigma^2_{kj} + \frac{(x^*_j - \mu_{kj})^2}{\sigma_{kj}^2} \big]$，参数个数 $2p$。
- （通用形式，非某本教材独有；判别分与 $2p$ 结论即 PSL §9.6 原文。）

**中文解释**

朴素贝叶斯假设「给定类别后特征相互独立」，把 $p$ 维联合密度拆成 $p$ 个一维密度相乘，因此参数从 $O(p^2)$ 降到 $O(p)$（高斯版只需 $2p$ 个）。这层独立性假设可以理解为高维下的正则化。工程上注意很多实现的 log-pdf 截断会导致数值问题。

**面试怎么用**

「为什么叫 naive」→ 条件独立假设；「参数多少」→ $2p$；再加一句「这其实是 high-dimensional 下的 regularization」，这是 PSL 的独特视角。

---

## E. 逻辑回归

### E24. 逻辑回归的设定（setup）与 logit link

**教材原文 · PSL §10.1 Setup（页面 w10_1_setup.html）**
（<https://liangfgithub.github.io/PSL/w10/w10_1_setup.html>）

> "As we have learned before, in the binary case, the best classifier depends on $\eta(x)= P(Y=1 | X=x).$ One type of approaches for classification is to directly model or estimate \(\eta(x)\) . Since $\eta(x)$ is constrained to between $0$ and $1$, as it represents a probability. Therefore, it's challenging to model $\eta(x)$ directly with a linear model because linear models are unconstrained. Instead, we model its transformation (or referred to as a link function ) with a linear model: $g(\eta(x)) = x^t \beta.$ Here, for simplicity, we incorporate the intercept into \(x\) , so \(\beta\) represents all the coefficients, including the intercept. In logistic regression, we use the so-called logit link function, which is equal to $\text{logit}(\eta(x)) = \log \frac{\eta(x)}{1 - \eta(x)}.$"
>
> 紧接的三句说明 logit 值域（**逐字**）：
>
> - "This transformation ensures that the logit value is unconstrained: when \(\eta(x) = 0.5,\) the ratio inside the log is 1, resulting in a logit of 0."
> - "When \(\eta(x) > 0.5,\) the ratio is greater than 1, yielding a positive logit ranging from 0 to infinity."
> - "When \(\eta(x) < 0.5,\) the ratio is less than 1, resulting in a logit ranging from -infinity to 0."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $\eta(x) = P(Y=1 \mid X=x)$；$g(\eta(x)) = x^t \beta$，logistic 回归取 logit link：$\text{logit}(\eta(x)) = \log \frac{\eta(x)}{1 - \eta(x)} = x^t \beta$。
- 反函数（sigmoid）：$\eta(x) = \frac{\exp(x^t \beta)}{1+\exp(x^t \beta)} = \sigma(x^t\beta)$，$\sigma(z) = e^z/(1+e^z)$。
- （通用形式，非某本教材独有；与 PSL §10.1/§10.2 原文记法一致。）

**中文解释**

分类的最优规则只依赖 $\eta(x)=P(Y=1|X=x)$，但 $\eta$ 被限制在 $[0,1]$，不能直接用无约束的线性模型建模；于是改成对它的变换（link function）建线性模型。logistic 回归选 logit：$\log\frac{\eta}{1-\eta}=x^t\beta$，值域覆盖整个实数轴，且 $\eta=0.5 \Leftrightarrow$ logit $=0$。

**面试怎么用**

被问「逻辑回归是什么」时，按 $\eta$ 有界 → link function → logit 三步讲，最后给出 logit 值域的三条性质（0.5 时 0、大于 0.5 正、小于 0.5 负）。

---

### E25. MLE（为什么用似然而不是 L2 损失）

**教材原文 · PSL §10.1 Setup 末段、§10.2 MLE（页面 w10_1_setup.html / w10_2_mle.html）**
（<https://liangfgithub.github.io/PSL/w10/w10_2_mle.html>）

> "While one might consider using the L2 loss $(y - \eta(x))^2$ , it has some limitations."
>
> "Squaring it further reduces the scale of the loss, resulting in small gradients, making training difficult and potentially leading to getting stuck at certain points."
>
> "Instead, we opt for the log-likelihood as our loss function."
>
> "We unify the expressions above for both Y=1 and Y=0 into a single form, using the sigmoid function, denoted as $\sigma(z) = e^z / (1 + e^z),$ where $z$ is shorthand for $x^t \beta$"
>
> "Next, we need to find the maximum likelihood estimate (MLE) for $\beta.$ We follow the standard approach of setting the gradient of the log-likelihood with respect to $\beta$ to zero and solving for $\beta.$"
>
> "During this iterative process, we also need to calculate the second derivative of the log-likelihood, which forms a matrix known as the Hessian matrix. The Hessian matrix is negative semi-definite, indicating that the log-likelihood function is concave. This property simplifies optimization, as any local maximum is also the global maximum."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 伯努利似然 $P(Y = y \mid X = x) = \sigma(z)^y (1 - \sigma(z))^{1-y}$，$\sigma(z) = \frac{e^z}{1 + e^z}$，$z = x^t \beta$。
- 对数似然 $\ell(\beta) = \sum_{i=1}^n \big[ y_i x_i^t\beta - \log\big(1 + \exp(x_i^t\beta)\big) \big]$（通用形式）；MLE 解 $\nabla \ell(\beta) = \mathbf{X}^t(\mathbf{y} - \mathbf{p}) = \mathbf{0}$，无闭式解。
- Hessian $\frac{\partial^2 \ell}{\partial \beta \partial \beta^t} = -\mathbf{X}^t \mathbf{W} \mathbf{X}$（$\mathbf{W} = \text{diag}(\eta_i(1-\eta_i))$），负半定 ⇒ 对数似然凹 ⇒ 局部最优即全局最优；用 Newton–Raphson / IRLS 求解。
- （通用形式，非某本教材独有；$P(Y=y|X=x)$ 与 Hessian 结论即 PSL §10.2 原文。）

**中文解释**

逻辑回归用 MLE：最大化伯努利似然（等价最大化对数似然），而不是最小化 $(y-\eta(x))^2$。原因很实际：$y-\eta$ 本来就小于 1，平方后梯度太小、训练容易卡住。对数似然对 $\beta$ 是凹函数（Hessian 负半定），所以任何局部最优就是全局最优，用 Newton–Raphson 迭代求解。

**面试怎么用**

被问「为什么不用 MSE」时，答两件事：尺度问题（gradient 太小）＋概率模型下似然更自然；再补 Hessian 负半定保证解唯一。

---

### E26. 分离数据（separable data）与正则化能否解决

**教材原文 · PSL §10.3 Seperable Data、§10.3.1 Can Regularization Help?**
（<https://liangfgithub.github.io/PSL/w10/w10_3_seperable.html>）

> "Herein lies the problem: when data are well separated, there is no limit to how large you can make the coefficients to push the probabilities in the desired direction. This causes logistic regression algorithms to fail to converge."
>
> "While there may be multiple coefficient sets, the decision boundary, the line that separates the classes, is still well-defined."
>
> "It’s important to clarify that using regularization techniques such as Ridge or Lasso won’t resolve the convergence issue encountered in this specific scenario."
>
> "For instance, in the two-dimensional toy example, even if we apply L1 or L2 penalties, cross-validation will reveal that the optimal choice for the regularization parameter (lambda) is zero."
>
> "The root cause of this issue is as follows: when the true classification rule is flawless, meaning that Y=1 with a probability of 1 when $x^t \beta > \beta_0,$ the model parameters $(\beta_0, \beta)$ become undefined, tending toward positive or negative infinity."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 完全分离：存在 $\beta$ 使 $x_i^t \beta > 0$ 对所有 $y_i = 1$、$x_i^t \beta < 0$ 对所有 $y_i = 0$ 成立；则似然在 $\|\beta\| \to \infty$ 方向上单调趋向上界 1，MLE 不存在有限解。
- 判据：分离 ⇔ 存在 $\beta$ 使 $y_i \cdot x_i^t \beta > 0$ 对所有 $i$ 成立（正线性可分）。
- （通用形式，非某本教材独有；PSL §10.3 用 2 维玩具例 + 「系数可以一直增大而似然持续变大」来论证同一件事。）

**中文解释**

当数据能被一个超平面完美分开（完全分离）时，把系数成倍放大总能继续提升似然，似然没有上界可及的最大值，于是系数发散、算法不收敛（R 会给 non-convergence 警告）。但决策边界（例如 $X_1+X_2=0$）仍然稳定，所以预测还能用，坏掉的是对 $\beta$ 的推断。PSL 明确说：ridge/lasso 也不解决这个问题，因为 CV 会选 $\lambda=0$——根因不是过拟合。

**面试怎么用**

高频追问，答「现象（系数无界/不收敛）→ 原因（完美可分时参数无定义）→ 影响（边界仍稳，推断失效）→ 处方（不是常规正则化；Firth/惩罚似然或贝叶斯先验）」。

---

### E27. 回顾性抽样（retrospective / case-control sampling）

**教材原文 · PSL §10.5.3 Retrospective Sampling、§10.5.4 The Challenge（页面 w10_5_sampling.html）**
（<https://liangfgithub.github.io/PSL/w10/w10_5_sampling.html>）

> "Typically, in machine learning, we assume that our data consists of independently and identically distributed (i.i.d.) samples drawn from a population of interest."
>
> "An alternative approach is to select a hundred patients but ensure that they comprise a balanced representation. In this case, we might draw 50 samples from a group of cancer patients and 50 from a control group, representing individuals without cancer. Consequently, our training dataset comprises 50 samples from each category."
>
> "Introduce an indicator variable, denoted as “Z,” to determine whether an individual has been sampled. Consequently, when analyzing retrospective samples, our obtain an estimate of the probability $P(Y=1 | Z=1, X=x).$ However, our ultimate goal is to learn the probability"
>
> "It’s interesting to note that for logistic regression models, the coefficients estimated from a retrospective sample are almost the same as those obtained from an ordinary random sample except the intercept."
>
> "This finding underscores the robustness of logistic regression in handling retrospective data, with the primary distinction often being the intercept, which can be adjusted to align the model with the characteristics of the broader population."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 抽样指示变量 $Z$：回顾性样本估的是 $P(Y=1 \mid Z=1, X=x)$，目标却是 $P(Y=1 \mid X=x) = \frac{\exp(\alpha + x^t \beta)}{1 + \exp(\alpha + x^t \beta)}$。
- 若病例组抽样比例 $\tau_1 = P(Z=1 \mid Y=1)$、对照组 $\tau_0 = P(Z=1 \mid Y=0)$，则 $\hat\alpha_{\text{pop}} = \hat\alpha_{\text{sample}} + \log(\tau_0/\tau_1)$（斜率不变、只平移截距；通用形式）。
- （PSL §10.5.4 以「slopes 几乎相同、只有 intercept 不同」给出同一结论，未给出修正公式。）

**中文解释**

回顾性（病例对照）抽样是按结果 $Y$ 抽样的：比如各抽 50 个病人与 50 个对照，样本里两类比例是 50-50，而总体不是。这违反了「训练与测试同分布」的常规假设，但对逻辑回归友好——斜率的估计几乎不变，只有截距需要按已知的抽样比例调整。

**面试怎么用**

保险/风控场景常问：可答「logistic 是少数几种在 case-control 下仍稳的模型，slopes 不变、intercept 可校正」，并强调预测总体概率前必须校正截距。

---

## F. 保险 / 精算（LDA）

### F28. GLM 框架

**教材原文 · LDA §8.4 Further Resources and References + LDA 词汇表（glossary）条目 "generalized linear model"**
（<https://openacttexts.github.io/Loss-Data-Analytics/ChapRiskClass.html>）

> "Poisson regression is a special member of a more general regression model class known as the generalized linear model (GLM). The GLM develops a unified regression framework for datasets when the response variables are continuous, binary or discrete. The classical linear regression model with a normally distributed error is also a member of the GLM."
>
> "… an extension of the linear regression model where the dependent variable is a member of the linear exponential family. glm encompasses linear, binary, count, and long-tailed, regressions all as special cases."
>
> "As another important feature, the Tweedie distribution is a special case of exponential dispersion A set of distributions that represents a generalisation of the natural exponential family and also plays an important role in generalized linear models. models, a class of models used to describe the random component in generalized linear models"
>
> "In insurance we are interested in total claims paid, whose distribution results from compounding the fitted frequency distribution with a severity distribution."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 三要素（通用形式）：随机成分 $Y_i \sim$ 指数散布族（exponential dispersion family），$\mathrm{E}[Y_i] = \mu_i$，$\mathrm{Var}(Y_i) = \phi V(\mu_i)$；系统成分（线性预测子）$\eta_i = x_i^t \beta$；连接函数 $g(\mu_i) = \eta_i$。
- 常见组合：Normal 恒等连接、Bernoulli logit 连接、Poisson/NB log 连接、Gamma 倒数连接、Tweedie（复合 Poisson–Gamma）log 连接。
- （通用形式，非某本教材独有；LDA 只文字性描述「统一回归框架」，未给出三要素公式。）

**中文解释**

GLM 是把线性回归推广到「响应变量属于指数散布族」的统一框架：随机成分给定分布（方差随均值变化 $\text{Var}=\phi V(\mu)$），系统成分是线性的 $x^t\beta$，两者用连接函数 $g$ 相连（count 用 log、binary 用 logit）。线性回归是它的特例。保险里频率（Poisson/NB）、严重度（Gamma）、纯保费（Tweedie）都用它。

**面试怎么用**

被问「GLM 和线性回归什么关系」时，用原文一句 "the classic linear regression model with a normally distributed error is also a member of the GLM"，再补三要素。

---

### F29. Poisson 回归与 offset（exposure）

**教材原文 · LDA §8.2.2 Poisson Regression、§8.2.3 Incorporating Exposure**
（<https://openacttexts.github.io/Loss-Data-Analytics/ChapRiskClass.html>）

> "We now describe Poisson regression in a formal and more general setting."
>
> "The condition inside the expectation in equation (8.10) indicates that the loss frequency $\mu_i$ is the model expected response to the given set of risk factors or explanatory variables."
>
> "The standard choice for Poisson regression is to adopt the exponential function, as we mentioned previously, so that"
>
> "In the regression literature, the values $x_{i1}, \ldots, x_{ik}$ are generally known as explanatory variables , as these are measurements providing information about the variable of interest"
>
> "Adding $\log m_i$ in (8.15) does not pose a problem in fitting as we can always specify this as an extra explanatory variable, as it is a known constant, and fix its coefficient to 1. In the literature the log of exposure, $\log m_i$ , is commonly called the offset"
>
> "For example, when we say the expected claim frequency of a health insurance policy is 0.2, it does not mean much without the specification of the exposure such as, in this case, per month or per year."

**标准记法**（通用形式，非某本教材独有，除非注明）

- $y_i \sim Pois(\mu_i)$，$\mu_i = \mathrm{E}(y_i \mid \mathbf{x}_i) = m_i\, e^{\mathbf{x}_i^t \beta}$；取对数 $\log \mu_i = \log m_i + \mathbf{x}_i^t \beta$，其中 $\log m_i$ 是 offset（系数固定为 1）。
- Poisson 过程参数化：$\Pr(Y=y) = \frac{(\lambda t)^y e^{-\lambda t}}{y!}$，$\mathrm{E}(Y) = \mathrm{Var}(Y) = \lambda t$，$\lambda$ 为 rate、$t$ 为 exposure。
- （公式即 LDA 式 (8.11)–(8.15) 原文；「offset 系数固定为 1」亦为原文表述。）

**中文解释**

Poisson 回归用对数连接把计数 $y_i$ 的期望写成 $\mu_i = e^{x_i^t\beta}$，这样 $\mu$ 永不为负。不同保单暴露期不同，就把 $\log m_i$ 作为 offset 加进线性预测子（系数固定为 1），这样建模的是「单位暴露的率」$\lambda_i$ 而不是计数本身。注意 offset 与普通变量的区别：offset 是已知常数项、不估系数。

**面试怎么用**

被问「exposure 怎么处理」时，直接给 $\log \mu_i = \log m_i + x_i^t\beta$ 并强调 offset 的系数是 fix 成 1 的；再补 rate/intensity 与 exposure base 的概念。

---

### F30. 乘性费率 / relativity（相对数）

**教材原文 · LDA §8.3.1 Rating Factors and Tariff、§8.3.2 Multiplicative Tariff Model**
（<https://openacttexts.github.io/Loss-Data-Analytics/ChapRiskClass.html>）

> "Thus we can imagine that, with a small number of rating factors, there will be many policyholders falling into the same risk class, charged with the same premium."
>
> "Each unique combination of the rating factors in a tariff is called a tariff cell"
>
> "The multiplicative form (8.16) is easy to understand and use, because it clearly shows how the expected loss count (per unit exposure) changes as each rating factor varies."
>
> "This over-parametrization, meaning that many different sets of parameters arrive at an identical model, obviously calls for some restriction on $f_{1j}$ and $f_{2k}$"
>
> "The standard practice is to make one relativity in each rating factor equal to one."
>
> "… is generally interpreted as the Poisson rate of the base tariff cell"
>
> "In non-life insurance, the parameters $f_{1j}$ and $f_{2k}$ are known as relativities"

**标准记法**（通用形式，非某本教材独有，除非注明）

- 乘性费率模型：$\lambda_{jk} = f_0 \times f_{1j} \times f_{2k}$；取对数 $\log \lambda_{jk} = \log f_0 + \log f_{1j} + \log f_{2k}$（即泊松回归的线性预测子，$\log f$ 扮演 $\beta$）。
- 基准类约束：每个 rating factor 的一个 relativity 固定为 1（如 $f_{11} = 1$、$f_{21} = 1$），base tariff cell 的费率 $\lambda_{11} = f_0$ 为基准值。
- 实际费率（relativity 的用法）：$\text{rate} = f_0 \times \prod_{\text{factor}} f_{\text{factor, level}}$。
- （公式即 LDA 式 (8.16)–(8.18) 原文。）

**中文解释**

乘性费率假设各 rating factor 对预期损失的影响相乘：$\lambda_{jk}=f_0\times f_{1j}\times f_{2k}$，每个 $f$ 就是 relativity（相对基准类的倍数）。因为相乘会「过度参数化」（分子分母可任意配对），每个因子必须把一个水平的 relativity 设为 1 作为基准。取对数后就是普通的泊松回归，$\log f$ 对应 $\beta$。默认假设因子间无强交互。

**面试怎么用**

被问「relativity 跟 GLM 系数什么关系」时，答：relativity $= e^{\beta}$，base level 的 relativity 固定为 1、截距 $f_0$ 是基准单元的费率；并主动指出乘性模型隐含「无交互」的假设。

---

### F31. Tweedie 分布（复合 Poisson–Gamma）

**教材原文 · LDA §5.3.4 Tweedie Distribution（ChapAggLossModels.html）**
（<https://openacttexts.github.io/Loss-Data-Analytics/ChapAggLossModels.html>）

> "In this section, we examine a particular compound distribution where the number of claims has a Poisson distribution and the amount of claims has a gamma distribution."
>
> "The Tweedie distribution has a mass probability at zero and a continuous component for positive values. Because of this feature, it is widely used in insurance claims modeling, where the zero mass is interpreted as no claims and the positive component as the amount of claims."
>
> "It is also worth mentioning the two limiting cases of the Tweedie model: $p\rightarrow 1$ results in the Poisson distribution and $p\rightarrow 2$ results in the gamma distribution."
>
> "This allows us to use the Tweedie distribution with generalized linear models to model claims."
>
> "Thus, the Tweedie distribution can be thought of a mixture of zero and a positive valued distribution, which makes it a convenient tool for modeling insurance claims and for calculating pure premiums."

**标准记法**（通用形式，非某本教材独有，除非注明）

- 复合模型 $S_N = X_1 + \cdots + X_N$，$N \sim \text{Poisson}(\lambda)$，$X_i \sim \text{Gamma}(\alpha, \gamma)$；零质量 $\Pr(S_N = 0) = \Pr(N = 0) = e^{-\lambda}$。
- 均值和方差（$\alpha$、$\gamma$ 参数化）：$\mathrm{E}(S_N) = \lambda \frac{\alpha}{\gamma}$，$\mathrm{Var}(S_N) = \lambda \frac{\alpha(1+\alpha)}{\gamma^2}$。
- GLM 参数化（$\mu$ 为均值、$\phi$ 为分散参数、$1 < p < 2$）：$\lambda = \frac{\mu^{2-p}}{\phi(2-p)}$，$\alpha = \frac{2-p}{p-1}$，$\frac{1}{\gamma} = \phi(p-1)\mu^{p-1}$，于是 $\mathrm{E}(S_N) = \mu$，$\mathrm{Var}(S_N) = \phi\mu^{p}$。
- （公式即 LDA §5.3.4 原文；$p \to 1$ 得 Poisson，$p \to 2$ 得 Gamma。）

**中文解释**

Tweedie（复合 Poisson–Gamma）＝索赔次数服从 Poisson、每次索赔额服从 Gamma 的复合分布。它在 0 处有离散质量 $e^{-\lambda}$（没出险），正半轴连续（出险则长尾），正好匹配「一堆零 + 一个正的长尾」的保单级纯保费数据，所以在 $1<p<2$ 时能给 GLM 当分布族用。$p\to1$ 退化为 Poisson，$p\to2$ 退化为 Gamma。

**面试怎么用**

保险/精算题常问「为什么用 Tweedie 而不是两阶段模型」：一次 GLM 同时给出「有没有出险」和「出了多少钱」，方差函数 $\text{Var}=\phi\mu^p$ 也自动处理零膨胀与长尾。

---

### F32. 严重度分布（Gamma / Pareto / Weibull）

**教材原文 · LDA §3.2.1 Gamma、§3.2.2 Pareto、§3.2.3 Weibull（ChapSeverity.html）**
（<https://openacttexts.github.io/Loss-Data-Analytics/ChapSeverity.html>）

> "When frequency and severity are modeled separately it is common for actuaries to use the Poisson distribution (introduced in Section 2.2.3.2 ) for claim count and the gamma distribution to model severity."
>
> "The continuous variable $X$ is said to have the gamma distribution with shape parameter $\alpha$ and scale parameter $\theta$ if its probability density function is given by"
>
> "Since all moments exist for any positive $k$ , the gamma distribution is considered a light tailed distribution"
>
> "It is a positively skewed and heavy-tailed distribution which makes it suitable for modeling income, high-risk insurance claims and severity of large casualty losses."
>
> "… raw moment of the Pareto distributed random variable exists, if and only if, $\alpha > k$"
>
> "It can be easily seen that the shape parameter $\alpha$ describes the shape of the hazard function of the Weibull distribution. The hazard function is a decreasing function when $\alpha < 1$ (heavy tailed distribution), constant when $\alpha = 1$ and increasing when $\alpha > 1$ (light tailed distribution)."

**标准记法**（通用形式，非某本教材独有，除非注明）

- Gamma$\,(\alpha, \theta)$：$f_X(x) = \frac{(x/\theta)^{\alpha}}{x\,\Gamma(\alpha)}\exp(-x/\theta)$，$\mathrm{E}(X) = \alpha\theta$，$\mathrm{Var}(X) = \alpha\theta^2$（轻尾，所有矩存在）。
- Pareto$\,(\alpha, \theta)$（两参数，LDA 式 (3.1)）：$f_X(x) = \frac{\alpha\theta^{\alpha}}{(x+\theta)^{\alpha+1}}$，$\mathrm{E}(X) = \frac{\theta}{\alpha-1}$（$\alpha>1$），第 $k$ 阶矩存在的条件为 $\alpha > k$。
- Weibull$\,(\alpha, \theta)$：$f_X(x) = \frac{\alpha}{\theta}\big(\frac{x}{\theta}\big)^{\alpha-1}\exp\big(-(x/\theta)^{\alpha}\big)$，$\mathrm{E}(X) = \theta\,\Gamma(1 + 1/\alpha)$；危险率随 $\alpha<1$ 递减、$\alpha=1$ 恒定、$\alpha>1$ 递增。
- （公式即 LDA §3.2 原文。）

**中文解释**

严重度建模常用三者：Gamma（右偏、轻尾，所有矩存在，是「频率 Poisson + 严重度 Gamma」的经典搭配）、Pareto（重尾，$k$ 阶矩仅在 $\alpha>k$ 时存在，适合大额赔案/极端损失）、Weibull（形状参数控制危险率单调方向，可靠性/寿命数据常用）。选哪个，本质是选尾部：重尾选 Pareto 类，否则 Gamma/Weibull。

**面试怎么用**

被问「赔额用什么分布」时，用尾重做主线：Gamma light tailed（all moments exist）vs Pareto heavy tailed（$\alpha>k$ 才有 $k$ 阶矩）；再补一句超额损失用广义 Pareto 拟尾部。

---

### F33. 频率分布（Poisson / 负二项 / 二项）

**教材原文 · LDA §2.2.3 Important Frequency Distributions（ChapFrequency-Modeling.html）**
（<https://openacttexts.github.io/Loss-Data-Analytics/ChapFrequency-Modeling.html>）

> "In this sub-section we study three important frequency distributions used in statistics, namely the binomial, the Poisson, and the negative binomial distributions."
>
> "These distributions can be motivated by natural random experiments which are good approximations to real life processes from which many insurance data arise."
>
> "Note that interestingly for the Poisson distribution $\mathrm{Var}[N]=\mathrm{E}[N]$"
>
> "We note that when $\beta>0$ , we have $\mathrm{Var}[N] >\mathrm{E}[N]$ . In other words, this distribution is overdispersed"
>
> "If we instead consider the number of successes until we observe the $r$ -th failure in independent repetitions of an experiment with binary outcomes, then its distribution is a negative binomial distribution."
>
> "… denotes the number of heads in a sequence of $m$ independent coin tossing experiments with an identical coin which turns heads up with probability $q$ , then the distribution of $N$ is called the binomial distribution"

**标准记法**（通用形式，非某本教材独有，除非注明）

- Binomial$(m, q)$：$p_k = {m \choose k} q^k (1-q)^{m-k}$，$\mathrm{E}[N] = mq$，$\mathrm{Var}[N] = mq(1-q)$（低散布：均值大于方差）。
- Poisson$(\lambda)$：$p_k = \frac{e^{-\lambda}\lambda^k}{k!}$，$\mathrm{E}[N] = \mathrm{Var}[N] = \lambda$；是 $m \to \infty$、$mq \to \lambda$ 时二项分布的极限。
- Negative Binomial$(r, \beta)$：$p_k = {r+k-1 \choose k}\big(\frac{1}{1+\beta}\big)^r \big(\frac{\beta}{1+\beta}\big)^k$，$\mathrm{E}[N] = r\beta$，$\mathrm{Var}[N] = r\beta(1+\beta)$（$\beta>0$ 时过散布）。
- （公式即 LDA §2.2.3.1–2.2.3.3 原文。）

**中文解释**

保险索赔次数的三大计数分布：二项（固定 $m$ 次试验的成功数，均值 > 方差，低频场景）、泊松（单位时间事件数，均值 = 方差）、负二项（$r$ 次失败前的成功数，$\text{Var} > \text{E}$，即过散布）。选择依据是方差-均值关系：过散布用 NB，低散布用二项，等散布用 Poisson；且三者都能方便地与严重度复合出总损失的分布。

**面试怎么用**

被问「频率数据用什么分布」时，先给方差-均值关系这条判据（$r\beta(1+\beta) > r\beta$、$mq(1-q) < mq$、$\lambda = \lambda$），再说 NB 是 Poisson 的过散布扩展（可理解为 Gamma 混合 Poisson）。

---

## 引用来源清单（抓取日期 2026-09-16）

| 来源 | 页面 / 章节 | 本文涉及条数 | URL | 抓取日期 |
| --- | --- | --- | --- | --- |
| PSL | §1.1.1 Types of statistical learning problems（w1_1_type_of_learning.html） | A1 | <https://liangfgithub.github.io/PSL/w1/w1_1_type_of_learning.html> | 2026-09-16 |
| PSL | §1.2.1 Introduction to LS and kNN + §1.2.4 Discussion | A2 | <https://liangfgithub.github.io/PSL/w1/w1_2_intro_LS_kNN.html> | 2026-09-16 |
| PSL | §1.2.4 Discussion（同页引文之一） | A2 | <https://liangfgithub.github.io/PSL/w1/w1_2_discussion.html> | 2026-09-16 |
| PSL | §2.1.1 / §2.1.3 Linear Regression | A3 | <https://liangfgithub.github.io/PSL/w2/w2_1.html> | 2026-09-16 |
| PSL | §2.2.1–§2.2.3 Column space / LS and projection | A4 | <https://liangfgithub.github.io/PSL/w2/w2_2.html> | 2026-09-16 |
| PSL | §2.3.5 Collinearity | A5 | <https://liangfgithub.github.io/PSL/w2/w2_3.html> | 2026-09-16 |
| PSL | §3.1 Subset Selection | B6 | <https://liangfgithub.github.io/PSL/w3/w3_1_subset.html> | 2026-09-16 |
| PSL | §3.2 Regularization | B7 | <https://liangfgithub.github.io/PSL/w3/w3_2_regularize.html> | 2026-09-16 |
| PSL | §3.3 Ridge Regression | B8 | <https://liangfgithub.github.io/PSL/w3/w3_3_ridge.html> | 2026-09-16 |
| PSL | §3.4 Lasso Regression | B9 | <https://liangfgithub.github.io/PSL/w3/w3_4_lasso.html> | 2026-09-16 |
| PSL | §3.5 Discussion | B10 | <https://liangfgithub.github.io/PSL/w3/w3_5_discussion.html> | 2026-09-16 |
| PSL | §4.1 Regression Trees（含 Pruning 3 小节） | C11, C12 | <https://liangfgithub.github.io/PSL/w4/w4_1_reg_tree.html> | 2026-09-16 |
| PSL | §4.2 Random Forest | C13 | <https://liangfgithub.github.io/PSL/w4/w4_2_randomforest.html> | 2026-09-16 |
| PSL | §4.3 GBM | C14 | <https://liangfgithub.github.io/PSL/w4/w4_3_gbm.html> | 2026-09-16 |
| PSL | §12.2 Impurity Measures | C15 | <https://liangfgithub.github.io/PSL/w12/w12_2_measures.html> | 2026-09-16 |
| PSL | §12.3 Misclassification Rate vs. Entropy | C16 | <https://liangfgithub.github.io/PSL/w12/w12_3_compare.html> | 2026-09-16 |
| PSL | §12.4 AdaBoosting | C17 | <https://liangfgithub.github.io/PSL/w12/w12_4_aAaboost.html> | 2026-09-16 |
| PSL | §12.5 Forward Stagewise Additive Modeling | C18 | <https://liangfgithub.github.io/PSL/w12/w12_5_boost.html> | 2026-09-16 |
| PSL | §5.1 Polynomial Regression | D19 | <https://liangfgithub.github.io/PSL/w5/w5_1_poly.html> | 2026-09-16 |
| PSL | §5.2 Cubic Splines | D20 | <https://liangfgithub.github.io/PSL/w5/w5_2_spline.html> | 2026-09-16 |
| PSL | §5.4 Smoothing Splines | D21 | <https://liangfgithub.github.io/PSL/w5/w5_4_smoothing_spline.html> | 2026-09-16 |
| PSL | §9.2 Discriminant Analysis | D22 | <https://liangfgithub.github.io/PSL/w9/w9_2_DA.html> | 2026-09-16 |
| PSL | §9.3 Quadratic Discriminant Analysis | D22 | <https://liangfgithub.github.io/PSL/w9/w9_3_QDA.html> | 2026-09-16 |
| PSL | §9.4 Linear Discriminant Analysis | D22 | <https://liangfgithub.github.io/PSL/w9/w9_4_LDA.html> | 2026-09-16 |
| PSL | §9.6 Naive Bayes Classifiers | D23 | <https://liangfgithub.github.io/PSL/w9/w9_6_NB.html> | 2026-09-16 |
| PSL | §10.1 Setup | E24, E25 | <https://liangfgithub.github.io/PSL/w10/w10_1_setup.html> | 2026-09-16 |
| PSL | §10.2 MLE | E25 | <https://liangfgithub.github.io/PSL/w10/w10_2_mle.html> | 2026-09-16 |
| PSL | §10.3 Seperable Data（含 §10.3.1 Can Regularization Help?） | E26 | <https://liangfgithub.github.io/PSL/w10/w10_3_seperable.html> | 2026-09-16 |
| PSL | §10.5 Retrospective Sampling Data | E27 | <https://liangfgithub.github.io/PSL/w10/w10_5_sampling.html> | 2026-09-16 |
| LDA | ChapRiskClass §8.2–§8.3、§8.4（含词汇表 glossary 条目） | F28, F29, F30 | <https://openacttexts.github.io/Loss-Data-Analytics/ChapRiskClass.html> | 2026-09-16 |
| LDA | ChapAggLossModels §5.3.4 Tweedie Distribution（并含 GLM 词汇表条目） | F31, F28 | <https://openacttexts.github.io/Loss-Data-Analytics/ChapAggLossModels.html> | 2026-09-16 |
| LDA | ChapSeverity §3.2 Severity Distributions | F32 | <https://openacttexts.github.io/Loss-Data-Analytics/ChapSeverity.html> | 2026-09-16 |
| LDA | ChapFrequency-Modeling §2.2.3 Important Frequency Distributions | F33 | <https://openacttexts.github.io/Loss-Data-Analytics/ChapFrequency-Modeling.html> | 2026-09-16 |
| ESL | §7.10 Cross-Validation, p. 244（1-SE 原则的定义，本地 ESLII_print12_toc.pdf 与官网 PDF 同版） | C12 | <https://hastie.su.domains/ElemStatLearn/> | 2026-09-16 |
| PSL | §1.1.1 Types of statistical learning problems（正文子页） | A1 | <https://liangfgithub.github.io/PSL/w1/w1_1_type_of_learning.html> | 2026-09-16 |
| PSL | §1.2.1 Introduction to LS and kNN（正文子页） | A2 | <https://liangfgithub.github.io/PSL/w1/w1_2_intro_LS_kNN.html> | 2026-09-16 |
| PSL | §1.2.x Discussion（正文子页；kNN 一致性的讨论） | A2 | <https://liangfgithub.github.io/PSL/w1/w1_2_discussion.html> | 2026-09-16 |

> [!tip] 逐字核对方式
> 本文件的英文引文由脚本从抓取到本地的页面正文（HTML→纯文本）中**按起止串截取**后直接写入，未经改写；核对时用 `grep -F` 在对应的 .txt 中回查。仅做了三类允许的清理：合并断行/多余空格、去掉 `&nbsp;`、把网页 LaTeX `\(...\)` 转写为 `$...$`（符号未改）。

## 相关

[[00 Index]] ・ [[04. 公式速查卡]] ・ [[优先级矩阵]] ・ [[事实核验报告]] ・ [[MAGNet 面试真题全解]]
