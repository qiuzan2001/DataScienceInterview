---
title: 10. 保险风控考点总览
chapter: 10
tags: [moc, insurance, index, interview]
status: 完成
updated: 2026-09-16
---

# 10. 保险风控考点总览

## 🎯 一句话
前 8 章讲的是「通用 ML 怎么算」，本章讲「保险公司为什么要这么算」——面试官问的不是公式，而是**你能不能把公式翻译成费率、准备金和风控决策**。

---

## 1. 本章 7 篇 × 优先级

| 篇 | 标题 | 优先级 | 一句话考点 | 允许不会？ |
|---|---|---|---|---|
| 10.1 | [[10.1 频率-严重度与纯保费]] | ⭐⭐⭐ **必背** | 纯保费 = 频率 × 严重度；保险公司为什么用 GLM 而不用 GBM 定费率 | 不行 |
| 10.2 | [[10.2 计数模型与 offset]] | ⭐⭐⭐ **必背** | `log μ = log(exposure) + x'β`，offset 系数固定为 1 | 不行 |
| 10.3 | [[10.3 严重度模型与 Gamma]] | ⭐⭐ 加分 | 严重度为什么用 Gamma/lognormal 而不是正态 | 能说结论即可 |
| 10.4 | [[10.4 评分卡与 WOE 落地]] | ⭐⭐⭐ **必背** | `score = A − B·ln(odds)`、PDO、WOE 单调性 | 不行 |
| 10.5 | [[10.5 模型评估与监控]] | ⭐⭐⭐ **必背** | KS / Gini=2·AUC−1 / Lift 十分位 / PSI / OOT | 不行 |
| 10.6 | [[10.6 采样偏差与拒绝推断]] | ⭐⭐ 加分 | 过采样后截距怎么校正、拒绝推断三种做法与风险 | 至少会截距校正 |
| 10.7 | [[10.7 SAS 实操速查]] | ⭐⭐ 加分 | PROC LOGISTIC / VARCLUS / MI / GENMOD / HPSPLIT 语句 | 可最后看 |

> 优先级标记含义同 [[00 Index]]：⭐⭐⭐ 必背（答不出直接减分）、⭐⭐ 加分（区分候选人层次）。

---

## 2. 通用 ML 知识库缺什么 / 保险面试多问什么

现有 1–8 章的内容本身没问题，但**默认语境是互联网/信贷**。保险面试官会把同一个知识点拧到保险语境上：

| 你已有的通用答案 | 保险面试想要的答案 | 差异点 |
|---|---|---|
| 「逻辑回归输出概率」 | 概率 → **纯保费 / 相对数 relativity**（`exp(β)`） | 要点出系数的乘性解释，而不是 odds ratio 说完就停 |
| 「Poisson 回归做计数」 | Poisson + **offset = log(exposure)**，系数固定为 1 | offset 这一条是保险面试的经典筛人题 |
| 「过散布就用负二项」 | 先分清 **过散布 vs 结构零（零膨胀）**，再选 NB / ZIP / hurdle | 保险数据零特别多（一年不出险） |
| 「不平衡就 SMOTE」 | 保险/风控**很少用 SMOTE**：会破坏概率校准，而定价必须校准 | 反直觉，答对加分明显 |
| 「AUC 0.8 模型很好」 | AUC 高 ≠ 定价好；看 **校准（预测频率 vs 实际频率）**+ 单调性 + OOT | 保险要的是真实频率，不是排序 |
| 「树模型效果好就用树」 | 费率模型要**可解释、可外推、单调、能报备** → GLM 为主，树做辅助 | 这是 10.1 的核心考点 |
| 「交叉验证」 | **OOT（out-of-time）**才是上线表现；还要注意赔付数据「发展期未满」 | 保险数据是时间序列 + 长尾发展 |
| 「KS 是评估指标」 | KS 的定义式、与 AUC 的关系、经验分档 | 风控面试的高频名词 |
| 「正则化解决分离」 | 完全分离：Ridge/Lasso 不能真正解决（CV 会选 λ=0），用 **Firth 惩罚** | 对应 [[1.6 Estimation Issues & Separation]] |

---

## 3. 1 天速成路径（面试前一天）

| 时段 | 内容 | 产出自检 |
|---|---|---|
| 上午 3h | [[10.1 频率-严重度与纯保费]] + [[10.2 计数模型与 offset]] | 能默写 `log μ_i = log m_i + x'_iβ`，能解释 offset 系数为什么是 1 |
| 中午 1h | [[10.3 严重度模型与 Gamma]] | 能背 Gamma vs lognormal 对比表的 3 行 |
| 下午 3h | [[10.4 评分卡与 WOE 落地]] + [[10.5 模型评估与监控]] | 能在纸上算出 A=487.1、B=28.85，能背 PSI 阈值 |
| 晚上 2h | [[10.6 采样偏差与拒绝推断]] + [[10.7 SAS 实操速查]] | 能背 `ln(99)=4.595` 的截距校正例；能默写 `PROC GENMOD` 的 offset 写法 |
| 睡前 30min | 只看每篇的「🗣️ 面试速答（30 秒）」 | 7 段话连起来讲一遍 |

**只背不练的代价**：10.4 的 A/B 换算和 10.6 的截距校正都能被要求「现场推一遍」，一定要亲手算一次。

---

## 4. 面试官最可能顺着问的 6 个问题

1. 你们做定价为什么用 GLM 而不是 XGBoost？→ 10.1
2. 保单暴露期不一样长怎么处理？→ 10.2（offset）
3. 索赔次数方差比均值大怎么办？→ 10.2（quasi-Poisson / NB / ZI）
4. 逻辑回归系数怎么变成分数？PDO 是什么？→ 10.4
5. KS 和 AUC 什么关系？上线后怎么监控模型失效？→ 10.5
6. 只用批准样本建模会有什么问题？→ 10.6

---

## 5. 来源（可引用）

- **Loss Data Analytics (LDA)**，开源精算教材：https://openacttexts.github.io/Loss-Data-Analytics/
  - Ch8 §8.2.3「Incorporating Exposure」→ offset=log(exposure) 与乘性费率；Ch8 新加坡车险数据（n=7,483）
  - Ch5 §5.3.4 Tweedie 分布；Ch3 严重度分布（§3.2.1 Gamma / §3.2.2 Pareto / §3.2.3 Weibull / §3.2.4 GB2）
  - Ch9 信度理论（limited fluctuation、Bühlmann、Bühlmann-Straub）；Ch10 风险度量（VaR/TVaR）与再保险；Ch11 链梯法与 Mack、GLM+Bootstrap 准备金
- **UIUC PSL（基于 ESL）**：https://liangfgithub.github.io/PSL/index.html
  - §10.5.3–10.5.4 回顾性/病例对照抽样（除截距外系数几乎不变）
  - §10.3 完全分离；§12.3 建树用熵/Gini、剪枝可用误分类率

> 本章不包含任何某公司内部流程或未公开数据；具体案例一律写「某车险公司」「示例数据」。所有经验阈值均标注为**经验值 / 行业常用，各机构不一致**。

---

## 🔗 相关
- [[00 Index]] —— 全库入口
- 通用基础：[[1.2 Generalized Linear Models (GLMs)]]、[[1.3 Introducing Logistic Regression]]、[[6.3 Performance Metrics]]
- WOE 口径主笔记：[[2.3.1 WOE & IV]]
